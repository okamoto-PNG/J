# Jリーグ 試合予想アプリ 解説（2026-27シーズン）

アプリは2つあります。どちらもダブルクリックで動きます。サーバー不要・インストール不要。

> **データ層（back）について**
> アプリが使うデータとパラメータは、すべて `data/` から `tools/build.js` が生成します。
> **HTMLを手で書き換えないでください。** 作り直す手順・データの形・検算の内容は
> [data/README.md](data/README.md) にまとめてあります。
> 迷ったら `node tools/verify.js` を実行してください（104項目の検算が通るかで健全性が分かります）。

| ファイル | 何をするか |
|---|---|
| [index.html](index.html) | **2クラブを選んで1試合を予想する**（単発型・J1のみ）。1〜11章はこちら |
| [節別予想.html](節別予想.html) | **毎節、結果を入力して次節を予想する**（週次型・J1とJ2）。12章以降はこちら |
| [publish/](publish/) | 上を Web に公開するための一式。26章以降はこちら |

節別予想アプリは、単発型と同じモデルを土台に「今季の結果を取り込んで戦力を更新する」
仕組みと、**J1・J2それぞれの2026-27シーズン全380試合の対戦カード**を載せたものです。

**手元で開くかぎり通信はしません。** ただし `publish/` に置いた公開版だけは、
今季の結果を Wikipedia から自動で取りに行きます（26章）。

---

## 1. これは何をしているアプリか

2026-27シーズンのJ1所属20クラブから2つ選ぶと、**勝敗の確率と予想スコア**を出します。

計算の材料は次の3つです。

| 要素 | 使っているデータ | 実測か |
|---|---|---|
| チームの攻撃力・守備力 | 2015-2025年 J1 全3588試合 | ✅ 実測 |
| ホームアドバンテージ | 同上（ホーム/アウェイ別に集計） | ✅ 実測 |
| 対戦相性（H2H） | 同カードの過去対戦 | ⚠ 実測したら**有害**だった（ほぼ無効化） |
| ACL・天皇杯・ルヴァンの日程負荷 | 全試合の試合日 | ⚠ 実測したら**効果なし**だった（使わない） |

**2026年8月に、データ層をすべて作り直しました。** それまでは Wikipedia の戦績表を
使っていて試合日がなく、日程負荷だけが「推測」でした。Ｊリーグ公式データサイトから
試合日つきのデータを取り直したことで、推測だった部分が実測に置き換わっています。
詳しくは [data/README.md](data/README.md) と 21章以降を見てください。

**予測精度は実測してあります。** 過去8シーズン2670試合で検証した結果：

```
的中率   45.8%   （常にホーム勝ちと予想した場合 41.2%）
log loss 1.0549  （同 1.0799 ／ 一様な1/3ずつだと 1.0986。小さいほど良い）
```

この数字は**試合日順のバックテスト**で出したものです（6章）。
以前ここに書いていた 1.0565 は、試合日が無かったため
「シーズンの試合をランダムに並べ替えて消化率を近似する」方法で測った値でした。
近似をやめた結果、比較できる同じ条件では 1.05585 → **1.05486** に改善しています。

正直に言うと**この程度です**。サッカーは本質的に読めない競技で、ブックメーカーの
オッズでも log loss は 1.0 前後です。「46%しか当たらない」ではなく
「ランダムより明確に良いが、断言できるものではない」と受け取ってください。

---

## 2. データはどこから来たか

**Jリーグには無料で使える公式APIがありません。** ただし API が無いことと
「機械で読めない」ことは別でした。調べた結果：

| 候補 | 結果 |
|---|---|
| football-data.org | J.League は収録されているが**有料プラン限定** |
| openfootball | 日本のデータなし |
| football-data.co.uk | 証明書エラー＋アクセス拒否で取得不可 |
| jleague.jp（試合ページ） | SPAなので全ページ同じHTMLを返す |
| Wikipedia API | ⚠ 取れるが**試合日が無い**（戦績表はクロス表なので日付を持たない） |
| **Ｊリーグ公式データサイト** | ✅ **これが正解。** 検索結果が素のHTMLで返る |

### 正解は公式データサイトだった

`https://data.j-league.or.jp/SFMS01/search?competition_years=2025&competition_frame_ids=1`

これだけで2025年J1の全380試合が、次の11列を持つ表として返ってきます。

```
シーズン / 大会 / 節 / 試合日 / K/O時刻 / ホーム / スコア / アウェイ / スタジアム / 入場者数 / 中継
```

**API を探すのに気を取られて、素のHTMLを返すページを見落としていました。**
「JSON で返る口」ではなく「機械で読める形で置いてあるか」を探すべきでした。
大会IDは J1=1・J2=2・J3=3・ルヴァン杯=11 です（天皇杯とACLはここには無い）。

### 旧データに2件の誤りが見つかった

Wikipedia 由来の3588試合と、公式データサイトの3588試合を突き合わせました。
**試合の組み合わせは3588件すべて一致し、スコアだけ2件食い違いました。**
公式記録（得点者と前後半のスコア）で確認したところ、どちらも公式が正しく、
Wikipedia 側の誤りでした。

```
2023 第1節  FC東京 2-0 浦和    （前半0-0／後半2-0）      ← Wikipediaは 2-1
2023 第3節  鳥栖   1-0 名古屋  （長沼洋一 83分の1点のみ）  ← Wikipediaは 2-0
```

**独立した2つの経路で同じものを見ると、誤りが見つかります。** これは13章で
日程データに使った手と同じ考え方です。1つのソースしか無いときは、
そのデータが正しいかどうかを確かめる方法がありません。

取得件数は全シーズンで理論値とぴったり一致しています。

```
2015: 306試合 (2ステージ制)   2021: 380試合 (20クラブ)
2016: 306試合 (2ステージ制)   2022: 306試合
2017: 306試合                 2023: 306試合
2018: 306試合                 2024: 380試合 (20クラブ)
2019: 306試合                 2025: 380試合 (20クラブ)
2020: 306試合                 ────────────────
                              合計 3588試合・重複0
```

集計した結果、平均2.59得点／ホーム勝率40.7%／引分率25.0% となり、
J1の一般的な水準と一致しました。データが正しく取れている証拠になります。

### 取得中に踏んだ罠

1. **2015・2016年は2ステージ制**で、節が `戦績表 (1st)` と `戦績表 (2nd)` に分かれていた
2. **`origin=*` を付けるとレート制限が厳しい**（429が連発）。ブラウザ用のCORS指定なので、
   Node から取るときは外す。加えて `User-Agent` を明示し、リクエスト間隔を空ける
3. **スコアの区切りがハイフンではなく en-dash（`–`）**。`/(\d+)-(\d+)/` では取れない

---

## 3. 予測モデルの中身

### 3-1. 基本はポアソン分布

サッカーの得点は「めったに起きないことが、ある平均回数で起きる」現象なので、
**ポアソン分布**でよく近似できます。平均得点 λ が分かれば、
「0点の確率」「1点の確率」…が計算できます。

```
P(k点) = e^(-λ) × λ^k / k!
```

そこで問題は「λ をどう見積もるか」に絞られます。

### 3-2. 攻撃力 × 守備力 × リーグ平均

```js
λ(ホームの得点) = リーグ平均ホーム得点 × ホームチームの攻撃力 × アウェイチームの守備力
λ(アウェイ得点) = リーグ平均アウェイ得点 × アウェイチームの攻撃力 × ホームチームの守備力
```

攻撃力・守備力は「1.0＝リーグ平均」となるように正規化した倍率です。
攻撃力1.3のチームは平均より3割多く点を取る、という意味になります。

### 3-3. ホームとアウェイを別々に持つ（← ご要望の「ホームアウェイの有利不利」）

ポイントは、各クラブについて**ホームでの成績とアウェイでの成績を別の数値として持つ**ことです。

```js
R[team] = { atkH, defH,   // ホームでの攻撃力・守備力
            atkA, defA }  // アウェイでの攻撃力・守備力
```

こうすると「ホームでだけ異常に強いクラブ」を表現できます。
リーグ全体のホームアドバンテージも実データから出ています。

```
リーグ平均得点  ホーム 1.376 / アウェイ 1.212  →  差 13.6%
```

同じ鹿島アントラーズ対浦和レッズでも、

```
鹿島ホーム → 鹿島の勝率 47.9%
浦和ホーム → 鹿島の勝率 35.8%      差 12.1ポイント
```

### 3-4. 時間減衰（古い試合は軽く見る）

11年前の成績と昨季の成績を同じ重みで扱うのはおかしいので、指数的に減衰させます。

```js
weight = 0.5 ** ((今季 - そのシーズン) / HALF_LIFE)
```

`HALF_LIFE = 6`（6シーズンで重みが半分）が最も精度が高くなりました。
**これは意外に長い値です。** 直近だけ見ると偏りすぎ、ということを意味しています。

ただし副作用があります。**川崎フロンターレが1位に来るのは2017-2021年の強さを
まだ引きずっているため**で、直近の姿とはズレる可能性があります。これは
「精度が最大になる設定」を選んだ結果の割り切りです。

### 3-5. 縮小推定（データが少ないクラブを平均に寄せる）

昇格直後で数試合しかないクラブの数値をそのまま使うと、偶然の連勝で
「攻撃力2.0」のような極端な値が出てしまいます。そこで
**架空の「平均的な試合」を K 試合分足してから割ります**。

```js
攻撃力 = (実際の得点 + K × リーグ平均) / (試合数 + K) / リーグ平均
```

`K = 28`。試合数が少ないほどリーグ平均（1.0）に引き寄せられ、
試合数が増えるほど実際の成績が反映されます。統計では **shrinkage** と呼ばれる
基本テクニックで、少数データの過剰解釈を防ぎます。

### 3-6. 対戦相性（H2H）— そして残念な実測結果

ご要望の「対戦の相性」です。実装は
**「総合力から期待される得点」と「実際にその相手から取った得点」のズレ**を見ます。

```js
相性倍率 = 1 + (実際 / 期待 - 1) × n/(n + H2H_K)
```

対戦数 n が少ないうちは効かせない（縮小推定）方式です。

**試合日順に測り直したら、相性補正は「効果ゼロ」ではなく「有害」でした。**

`H2H_K` は大きいほど補正が弱まります（∞で完全に無効）。値を動かすと：

```
H2H_K   学習区間(2018-22)   検証区間(2023-25)
  24        1.04672            1.06947      ← 従来
  32        1.04634            1.06898
  48        1.04608            1.06853
  80        1.04598            1.06822      ← 採用
   ∞        1.04608            1.06790
```

**弱めるほど、学習区間でも検証区間でも一貫して良くなります。**
単調に動くので偶然ではありません。改善幅 0.0013 は、このアプリで見つかった
どの変更よりも大きいものでした。

そこで `H2H_K = 80` にしました。実質ほぼ無効です（対戦20回でも効き幅は2割）。
完全な無効（∞）にしなかったのは、6章の採用規則（学習区間と検証区間の両方で
改善すること）を満たさなかったためです。∞は検証区間だけがさらに良くなります。

なぜ有害なのか。「A は B に相性が良い」と感じる現象の大半は
**単に A が B より強いこと**で説明でき、それはすでに攻撃力・守備力に入っています。
そこへ相性補正を重ねると、**同じ情報を二重に数えることになる**わけです。

以前ここには「効果はほぼゼロ（1.0565 対 1.0567）」と書いていました。
ランダム並べ替えによる近似では差が見えず、試合日順にしたら見えた、ということです。

### 3-7. Dixon-Coles 補正 — こちらも効かなかった

ポアソンだけだと 0-0 や 1-1 の引き分けが過小評価される、という有名な問題があり、
Dixon-Coles の低スコア補正（パラメータ ρ）が定番です。実装して当てはめたところ、
**J1では ρ = 0（補正なし）が最良**でした。コードは残してありますが既定は 0 です。

「定番の手法だから入れる」ではなく「効くか測ってから決める」のが正しい順序です。

---

## 4. 使っているパラメータ一覧

`tools/tune.js` が 2018-2025年の2670試合を**試合日順に**バックテストして決めた値です。
値は [data/params.json](data/params.json) に書かれ、アプリの定数は
`tools/build.js` がそこから生成します。**HTMLを手で書き換えないでください。**

| パラメータ | 値 | 意味 |
|---|---|---|
| `HALF_LIFE` | 6 | 6シーズンで重みが半分 |
| `SHRINK` | 28 | 縮小推定の強さ |
| `RHO` | 0 | Dixon-Coles 補正（不要と判明） |
| `H2H_K` | **80** | 相性補正の縮小。実質無効（3-6章。従来は24） |
| `H2H_CAP` | ±25% | 相性補正の上限（H2H_K=80 なので出番はほぼ無い） |
| `CUR_W` | **1.2** | 今季1試合の重み（14章。従来は1.5） |
| 日程補正 | **なし** | 実測で効果を検出できなかった（7章） |

### 採用の規則が変わりました

以前は「2670試合で log loss が最小になる値」を選んでいました。
これは**評価する試合そのものでパラメータを選ぶ**ことなので、
たまたま合っただけの値を掴んでしまいます（過剰適合）。

実際にやってみると、はっきり分かれました。

| 変更 | 学習区間(2018-22) | 検証区間(2023-25) | 判定 |
|---|---|---|---|
| `H2H_K` 24→80 | -0.00074 | -0.00125 | ✅ 本物 |
| `CUR_W` 1.5→1.2 | -0.00001 | -0.00011 | ✅ 本物（小さい） |
| `HALF_LIFE` 6→7 | -0.00000 | **+0.00009** | ❌ 過剰適合 |
| `SHRINK` 28→26 | -0.00000 | **+0.00020** | ❌ 過剰適合 |
| `RHO` 0→0.02 | -0.00005 | **+0.00004** | ❌ 過剰適合 |

そこで現在は
**「学習区間 2018-2022 と 検証区間 2023-2025 の両方で下がったときだけ採用する」**
という規則にしています。片方だけの改善は雑音として捨てます。
この規則なら、あとで誰が再実行しても同じ結論になります。

`node tools/validate.js` で、この表を自分で再現できます。

---

## 5. 昇格クラブの扱い

2026-27シーズンのJ1 20クラブのうち、**水戸ホーリーホックとジェフユナイテッド千葉は
2015-2025年のJ1に在籍歴がありません**（水戸は初昇格、千葉は2009年以来）。
つまり学習データがゼロです。

そこで「過去の昇格クラブは初年度にどれくらいの成績だったか」を実測して初期値にしました。
2016-2025年の昇格25クラブ・882試合から：

```
atkH = 0.8820   ホームでの攻撃力（リーグ平均の88%）
defH = 1.1013   ホームでの守備力（10%多く失点）
atkA = 0.8495   アウェイでの攻撃力
defA = 1.0389   アウェイでの守備力
```

公式データで測り直しても、標本は同じ**25クラブ・882試合**で、値も小数第3位までほぼ同じでした。
元の推定が正しかったことの裏付けになります（ズレは修正した2件のスコア分）。

### J2の成績を使う案は採用しませんでした

水戸と千葉が同じ値から始まるのは不満です。そこで J2 のデータ（2015-2026・5298試合）を
取ってきて、「J2でどれくらい強かったか」を持ち越す案を検証しました。

```
事前分布 = 昇格クラブ平均 + b × (J2での攻守力 - 1)
```

`b` を 0〜1 で振り、**昇格クラブが絡む試合だけ**（286試合）で log loss を測りました。
J2 側の見方（直近1年だけ見る／2年／6年）も4通り試しました。結果は全パターンで同じ形でした。

| b | 学習区間 | 検証区間 |
|---|---|---|
| 0.0 | 1.02420 | 1.12483 |
| 0.4 | 1.02745 | 1.11791 |
| 0.8 | 1.03183 | 1.11201 |
| 1.0 | 1.03445 | 1.10945 |

**学習区間では単調に悪化し、検証区間では単調に改善します。** 完全に逆です。
2018-2022の昇格クラブ（長崎・大分・横浜FC・徳島・京都）はJ2の勢いが続かず、
2023-2025（町田・東京V・岡山）は続いた、というだけの話でしょう。
**昇格8クラブ・286試合では、どちらが実態なのか決められません。**

回帰でも同じでした。相関は atkH で 0.13、defH で 0.03 しかなく、
defA では係数が **-2.05**（J2で守備が良かったクラブはJ1で失点が増える）という
意味を持たない値になりました。標本13クラブでは何も言えない、ということです。

4章の規則どおり、**採用しません**。
これは「やらなかった」ではなく「やって、効かないと確かめた」記録です。
`node tools/calibrate-promoted.js` で再現できます。

### ここで一度間違えました

最初は「昇格組の得点 1.118 ÷ リーグ平均 1.294 = 0.864」という**1つの値**を
攻撃力に使っていました。しかしこれは誤りです。

**ホームとアウェイでリーグ平均得点が違う**（1.373 対 1.207）ので、
同じ 0.864 を両方に当てると、アウェイの攻撃力を過大評価してしまいます。
ホーム・アウェイそれぞれの平均で割った4つの値に分けるのが正解でした。

このバグは「バッジに表示した精度と、アプリの実コードで測った精度が一致しない」
という形で発覚しました（1.0569 と表示していたが実測1.0601）。
**表示した数字を自分のコードで再検証する**ことで見つかったバグです。

なお両クラブは「昇格組の平均像」なので、実際の実力差は反映されていません。
アプリでも警告を出しています。

---

## 6. 精度の検証方法（ここが一番大事）

「動いた」と「当たる」は別です。次の手順で検証しました。

```
各シーズン Y について:
    Y の試合を日付順に並べる
    その日付ごとに:
        「Y より前の全シーズン」＋「Y のうちその日より前に終わった試合」で学習
        　　↑ 未来のデータを絶対に使わない（これを守らないと精度が嘘になる）
        その日の試合を予測して、実際の結果と比べる
```

これを2018〜2025の8シーズンで回し、2670試合分の予測を評価しました。
1回の評価は Node で **0.3秒**なので、パラメータ探索を何百通り回せます。

**試合日が手に入ったことで、この形にできました。** 以前は試合日が無かったので
「シーズンの試合をランダムに並べ替えて先頭m試合を消化済みとみなす」という近似でした
（14章）。ランダムな部分集合には終盤の試合も混じるので、序盤の予測に
終盤の情報が漏れます。近似をやめたので、その漏れがなくなりました。

### なぜ「的中率」だけでは不十分か

的中率は「最も確率が高い結果が当たったか」しか見ません。
「ホーム勝ち90%」と予想して外した場合と「40%」で外した場合が同じ扱いになります。

**log loss** は確率の確信度まで評価します。自信を持って外すと大きく罰せられます。
確率を出すモデルの評価には log loss（または Brier スコア）を使うのが基本です。

```
log loss = -平均( log(実際に起きた結果に付けた確率) )
```

### ベースラインと必ず比べる

```
一様（1/3ずつ）        log loss 1.0986  的中率 33.3%
基準率（常にホーム有利） log loss 1.0799  的中率 41.2%
本モデル               log loss 1.0549  的中率 45.8%
```

学習区間と検証区間に分けた値も出しています。

```
学習区間 2018-2022   log loss 1.0460
検証区間 2023-2025   log loss 1.0682
```

検証区間のほうが悪いのは、パラメータを選ぶのに使っていないぶんが正直に出るからです。
**実際にこれから当たる精度は、検証区間の 1.068 に近いと考えるべきです。**

**ベースラインを出さずに「的中率46%」と言っても意味がありません。**
比較対象があって初めて「良いのか」が判断できます。

---

## 7. 日程補正の扱い（ACL・天皇杯・ルヴァン）

ご要望の「ACLや天皇杯、ルヴァンカップの日程も加味」の部分です。
以前はここだけが実測ではなく、公表研究の一般的な水準に基づく調整パラメータでした。

```js
中2日 ×0.93 / 中3日 ×0.96 / 中4日 ×0.98 / 中5日 ×0.99 / 中6日以上 ×1.00
ACL（ホーム）×0.96 / ACL（アウェイ）×0.94 / 天皇杯・ルヴァン ×0.98
```

**試合日が手に入ったので実測しました。結論から言うと、効果は検出できませんでした。**

### 測り方

全3588試合に試合日があり、ルヴァン杯738試合も同じ形で取れています。
これで「各クラブが、その試合の何日前に公式戦を戦ったか」が分かります。

各試合の**素の期待得点**（モデルが出す λ）に対して、
**実際の得点が何倍だったか**を、休養日数で層別して集計します。
期待得点にはチームの地力が入っているので、
「連戦するのは強いクラブ」という偏りは打ち消されます。

### 結果

```
バケット      件数   得点比 (±)      失点比 (±)
中2日以下      648   1.014 ±0.034   0.945 ±0.034
中3日        1326   0.941 ±0.024   0.952 ±0.024
中4日         198   0.966 ±0.062   0.999 ±0.062
中5日         781   1.033 ±0.031   1.041 ±0.031
中6日         995   0.975 ±0.028   0.969 ±0.028
中7〜9日       455   0.983 ±0.041   1.039 ±0.041
中10日以上      799   0.974 ±0.031   0.973 ±0.031
```

想定していた「中2日は得点が7%落ちる」は**まったく見えません**（1.014、誤差±0.034）。

中3日で得点比・失点比がそろって 0.94〜0.95 に下がっているのは、
**J1の水曜開催では20クラブ全部が中3日になる**ためです。
両チームが等しく疲れるので試合全体の得点が減るだけで、
どちらが有利になるという話ではありません。

有利不利を見るには**相手との差**を見ます。ここが本題です。

```
相手との休養差       件数   得点比   失点比
3日以上少ない        503   1.012    0.961
1〜2日少ない        550   1.017    0.944
同じ              3096   0.978    0.978
1〜2日多い          550   0.944    1.017
3日以上多い          503   0.961    1.012
```

**休養が少ない側のほうが、わずかに良い成績です。** 想定と符号が逆でした。
ただし 503件では誤差 ±0.04 程度なので、**「差が無い」と読むのが正しい**でしょう。

### 決定的な判定：log loss が下がるか

表を眺めるだけでは決められないので、補正の効き幅 θ を振ってバックテストしました。
θ は「中2日のときに何%落とすか」です（θ=0.07 が従来の想定に相当）。

```
  θ      学習区間    検証区間
  0.00   1.04598    1.06822    ← 最良
  0.02   1.04617    1.06846
  0.04   1.04644    1.06875
  0.06   1.04680    1.06909
  0.10   1.04779    1.06995
```

**効かせるほど、両区間で単調に悪化します。** つまり従来の日程補正は
精度を下げていました。よって**日程補正は入れません**（θ=0）。

### アプリの日程パネルはどうなったか

既定値（中6日・カップなし）ではすべて ×1.00 なので、
**アプリの既定の予想は最初からこの実測結果と一致していました。**
パネルは「もし中2日なら」と手で仮定を置く what-if の欄として残し、
注記を「実測では効果が検出できなかった」に書き換えています。

### 天皇杯とACLが無いことの影響

天皇杯（JFA主催）とACL（AFC主催）の日程は取れていません。
JFA公式サイトはJavaScriptで描画するので素のHTMLから読めませんでした。

これがあると「本当は中3日なのに中6日と見えている」試合が混ざり、
効果が薄まって見える恐れがあります。**どれくらい混ざるのか測りました。**
手元にあるルヴァン杯をわざと隠して、休養日数がどれだけズレるかを見ます。

```
リーグ戦だけで測ると、5190件のうち719件（13.9%）で休養日数を過大に見積もる
```

1大会で約14%です。天皇杯・ACLも同じ桁でしょう。
仮に本当の効果が -7% あるなら、1〜2割の混入では消えません。上の表に必ず表れます。
**表れていないうえ符号すら逆向き**なので、「効果は無い」と判断しました。

`node tools/calibrate-schedule.js` で再現できます。

---

## 8. コードの構成

天気アプリと同じく3層に分けています。

```
[データ]  DATA（圧縮文字列） → MATCHES（3588件の配列）
[モデル]  fitRatings / h2hFactor / outcome / predict   …… 純粋関数。DOMを触らない
[描画]    renderResult / renderRanking                 …… stateを読んでHTMLを作る
```

モデル層が純粋関数なので、**ブラウザを開かずに Node だけで精度検証できます**。
実際に検証スクリプトはアプリのHTMLからモデル部分だけを抜き出して実行しており、
「表示している精度＝実際のコードの精度」であることを保証しています。

### データの埋め込み方

`file://` で開いたHTMLから別のローカルJSONを `fetch` すると
**CORSでブロックされます**。そのためデータはHTMLに直接埋め込んでいます。
1試合を `"季.ホーム.アウェイ.得点.失点"` の形に圧縮し、`;` で連結して約40KBです。

**この埋め込みは手で書くものではありません。** `data/*.json` を正として
`node tools/build.js` が生成します。`node tools/verify.js` が
「HTMLの埋め込み3588件と JSON が1試合単位で完全一致するか」を毎回確かめます。

```js
// "10.30.20.2.1" → 2025年 鹿島(ホーム) 2-1 浦和(アウェイ)
const MATCHES = DATA.split(";").map((r) => {
  const [s, h, a, hg, ag] = r.split(".").map(Number);
  return { s: 2015 + s, h: CLUBS[h], a: CLUBS[a], hg, ag };
});
```

---

## 9. このアプリの限界（把握しておくべきこと）

| 限界 | 影響 |
|---|---|
| `HALF_LIFE=6` が長い | 川崎Fが1位など、数年前の強さを引きずる |
| 選手情報なし | 主力の移籍・負傷・出場停止が一切反映されない |
| 監督交代なし | 戦術変更の影響を捉えられない |
| 水戸・千葉は履歴ゼロ | 昇格組の平均で代用。両クラブが同じ値から始まる |
| 天皇杯・ACLの日程がない | 休養日数の推定に1〜2割の取りこぼし（7章で影響を評価済み） |

**「試合日がない」と「J2の成績を使っていない」は解消しました。**
試合日は全3588試合に入り（2章）、J2も5298試合ぶん取得しています。
ただし J2 を昇格クラブの評価に使う案は、**検証したうえで採用しませんでした**（5章）。

試合日が入ったので「直近N試合の調子」は原理的に測れるようになりました。
7章で休養日数を測った結果が「効果なし」だったのと同じ手順で検証できます（22章）。

**特に選手情報の欠落は大きいです。** 得点源が移籍しても数値は変わりません。
「統計的な地力の比較」であって「今週末の予想」としては情報が足りていない、
と理解して使ってください。

---

## 10. 手を動かして改良する

### 難易度★

1. **引き分けの多いカードを目立たせる**
   → `renderResult()` で `dr > 0.30` のとき注記を出す
2. **両チームの得点期待値の合計から「オーバー2.5点」の確率を出す**
   → `p.grid` を走査して `i + j >= 3` の確率を合計する
3. **戦力ランキングを攻撃順・守備順で並べ替えられるようにする**

### 難易度★★

4. **リーグ順位表をシミュレーションする**
   → 20クラブの総当たり38節を各1万回試行し、優勝確率・降格確率を出す。
     `predict()` があるので、あとは乱数でスコアを引くだけ
5. **予想を保存して的中率を自分で記録する**
   → localStorage に予想と実際の結果を貯め、自分の log loss を計算する
6. **`HALF_LIFE` をスライダーで変えられるようにする**
   → 値を変えるとランキングがどう動くか見ると、モデルの感覚が掴めます

### 難易度★★★

7. ~~**試合日を含むデータを集めて、日程補正を実測に置き換える**~~
   → **完了。** 7章。実測したら効果は検出できず、補正を外しました
8. ~~**J2の成績を使って昇格クラブを評価する**~~
   → **完了（採用せず）。** 5章。標本8クラブでは学習区間と検証区間で結論が逆になりました
9. **モデルを Dixon-Coles の最尤推定に置き換える**
   → 現在は「得点比の集計」という簡易法。全パラメータを同時に最尤推定すると
     精度が上がる可能性があります。**必ずバックテストで確認してから採用してください**

まだやれることの一覧は 25章にまとめ直しました。

### 改良したら必ずやること

```
node tools/verify.js が全項目通ることを確認する。
そして log loss が「学習区間と検証区間の両方で」下がったか確認する。
片方だけなら、それは「良くなった気がする」だけです。
```

---

## 11. 注意

- このアプリは**統計モデルによる推定**であり、結果を保証するものではありません
- **賭博等の用途を意図していません**
- データ出典：Ｊリーグ公式データサイト（`data.j-league.or.jp`）の日程・結果
- 2026-27シーズンの所属クラブと日程は取得時点（2026年8月）の情報です
- データを取り直す手順は [data/README.md](data/README.md)

---
---

# 節別予想アプリ（[節別予想.html](節別予想.html)）

ここからは週次型の解説です。

## 12. 何ができるか

```
第1節が終わる → 10試合のスコアを入力する
              → 戦力が更新される
              → 第2節の予想が変わる
              → 順位表と「優勝/降格確率」も変わる
第2節が終わる → 繰り返し
```

入力した内容はブラウザの localStorage に自動保存されるので、翌週開き直せば続きから使えます。
別の端末に移したいときは「書き出し（JSON）」で文字列を取り出して貼り付けてください。

画面は4つです。

| 画面 | 中身 |
|---|---|
| 節ビュー | その節の10試合。未消化なら勝分敗の確率と予想スコア、消化済みなら結果 |
| 順位表 | 入力済みの結果から自動集計（勝点→得失点差→総得点） |
| 優勝・降格確率 | 残り全日程をモンテカルロで消化して最終順位を作る |
| クラブの状況 | 離脱者と「週中に別大会」の登録 |

---

## 13. 日程データをどう手に入れたか（ここが一番苦労した）

> **【2026年8月・追記】この章は「遠回りした記録」として残しています。**
> 2章の公式データサイトを使えば、380試合すべてが**日付つきで1リクエスト**で取れます。
>
> ```
> https://data.j-league.or.jp/SFMS01/search?competition_years=2026&competition_frame_ids=1
> → 380試合・38節・各節10試合・379試合に日付（残り1試合は「未定」）
> ```
>
> 現在のアプリはこちらを使っています。そして**下の方法で作った日程と突き合わせたら、
> 380試合すべてが完全に一致しました**。20クラブ分を突き合わせて作った日程は正しかった、
> ということです（`node tools/verify.js` が毎回確認します）。
>
> 以下は、その苦労と検算の考え方の記録です。検算の手筋そのものは今でも有効です。

**「誰と誰がいつ戦うか」が分からないと週次アプリは成立しません。**
1章で書いたとおりJリーグには無料の公式APIがなく、日程も例外ではありませんでした。

| 試したこと | 結果 |
|---|---|
| `jleague.jp/match/section/j1/1/` 〜 `/38/` を順に取得 | ❌ SPAなので**全38ページが同じHTML**を返す（中身はJSで描画） |
| Ｊリーグ公式の対戦カード発表PDF | ❌ 取得はできたが、この環境にPDFのテキスト抽出手段がなかった |
| Wikipedia「2026-27年のJ1リーグ」 | ❌ 記事が無い（404）。あっても戦績表は節を持たない |
| スポーツナビの日程ページ | ❌ 「データがありません」 |
| **クラブ別の日程ページ（20クラブ分）** | ✅ 静的HTML。1クラブ＝38節ぶん取れる |
| （後から発見）**公式データサイト** | ✅✅ 日付つきで1回で全部取れた |

最後の方法を採りました。ただし**1クラブからは自分の38試合しか取れません**。
380試合すべてを埋めるには20クラブ分が必要です（各試合が2クラブに現れるので、
20クラブぶん集めると同じ試合を2回ずつ見ることになります）。

### この「2回ずつ見る」のが検算に使える

取得したデータをそのまま信じるのは危険です。実際、20クラブ分を突き合わせたら
**13件の食い違い**が出ました。ほぼすべてが「どちらがホームか」の取り違えです。

```
第1節  kashima のページ … 鹿島 vs 横浜FM
       yokohamafm のページ … 横浜FM vs 鹿島      ← 逆になっている
```

ここで効くのが**総当たりの制約**です。38節の総当たり2回戦では

```
・各節にはちょうど10試合、20クラブが1回ずつ登場する
・「A(ホーム) 対 B(アウェイ)」という順序付きの組み合わせは、シーズン中ちょうど1回だけ
```

が必ず成り立ちます。上の例なら、鹿島と横浜FMのもう一方の対戦（第28節）は
両クラブのページが「鹿島 vs 横浜FM」で一致しています。すると
**「鹿島がホーム」はもう使われている**ので、第1節は自動的に「横浜FM がホーム」に決まります。

この論法で **13件のうち12件が一意に確定**しました。推測ではなく、制約から導かれた答えです。

残る1件は 川崎F 対 京都 で、第2節と第26節の両方が「京都(ホーム) 対 川崎F」になっており、
制約だけでは「どちらを裏返すか」が決まりませんでした。ここだけ両クラブのページの
ホーム/アウェイ表記と会場名（第2節＝等々力、第26節＝サンガS）を直接見て確定しています。

### 最終チェック

確定後、次の4つをすべて満たすことをスクリプトで検証しています。

```
✅ 380試合ちょうど
✅ どの節も10試合・20クラブが1回ずつ
✅ 順序付きの組み合わせ190×2種が重複なく全部そろう
✅ 各クラブ ホーム19試合・アウェイ19試合
```

第1節が報道された開幕カード（横浜FM 対 鹿島 ほか）と完全に一致することも確認しました。

### 教訓

**「取れた」と「正しい」は別です。**
今回は「同じ試合を2つの独立な経路で見る」構造があったので検算できました。
データを取ったら、そのデータ自身が満たすべき制約を書き出して機械に確かめさせる、
というのがいちばん確実です。

もうひとつの教訓は**「機械で読める形で置いてある場所を先に探せ」**です。
「無料APIが無い」で止まらず、素のHTMLを返すページを探すべきでした。
公式データサイトを見つけるまでに、20クラブ分のスクレイピングと
13件の食い違いの解決という遠回りをしています。

日程は760文字に圧縮してHTMLに直接埋め込んでいます。
各節の開催日も `ROUND_DATES` として一緒に埋め込んであります。

```js
// 2文字で1試合（ホームとアウェイのクラブ番号）、20文字で1節
const AB = "0123456789ABCDEFGHIJ";
h: J1[AB.indexOf(FIXTURES_RAW[w * 20 + i * 2])]
```

---

## 14. 今季の結果をどれくらい重く見るか（実測して決めた）

節別アプリの心臓部です。単発アプリの `fitRatings` は「今季より前」しか使いませんでしたが、
今回は今季の試合も足します。

```js
for (const m of MATCHES) add(m, Math.pow(0.5, (SEASON - m.s) / P.HALF_LIFE));  // 過去（減衰）
for (const m of cur)     add(m, P.CUR_W);                                     // 今季
```

問題は `CUR_W` です。**今季の1試合は、昨季の1試合の何倍の価値があるのか。**
直感的には「今のチームを表しているのだから重いはず」ですが、
一方で試合数が少ないうちは偶然の影響も大きく、重くしすぎると振り回されます。

### 以前は近似するしかなかった

かつての学習データ（Wikipedia の戦績表）には試合日も節番号もなく、
**「第5節まで消化した時点」を再現できませんでした**。そこで
「Y の試合をランダムに並べ替え、先頭 m 試合を消化済みとみなす」という近似で測り、
`CUR_W = 1.5` を採用していました。

**この近似には情報漏れがあります。** ランダムに選んだ m 試合には終盤の試合も混じるので、
序盤の予測に終盤の情報が入ってしまいます。そのぶん精度が良く見えます
（当時の全体値 1.05425 は、この漏れを含んだ数字です）。

### 試合日が入ったので、日付順で測り直しました

いまは各試合の日付が分かるので、6章の手順そのままで
「その日より前に終わった試合だけを重み `CUR_W` で足す」ことができます。近似は不要です。

```
CUR_W   学習区間(2018-22)   検証区間(2023-25)   全体
  0         1.04800            1.06915        1.05644   ← 今季を使わない
  0.8       1.04625            1.06830
  1.0       1.04608            1.06824
  1.2       1.04598            1.06822        1.05486   ← 採用
  1.5       1.04593            1.06825        1.05484   ← 従来
  2.2       1.04620            1.06853
  3.5       1.04752            1.06951
```

**`CUR_W = 1.2` にしました**（従来1.5）。読み取れることは3つあります。

1. **今季を使うと確かに良くなる。** 全体で 1.05644 → 1.05486（0.0016の改善）。
   学習・検証の両区間で一貫して下がるので本物です。
   近似で測っていた当時の値（0.0013）とほぼ同じ大きさで、ここは近似が妥当でした。
2. **最適な重みは平らで、1.0〜1.5 のどこでも大差ありません。**
   1.2 と 1.5 の差は 0.00002 しかなく、実質同じです。
   4章の採用規則（両区間で改善）に素直に従って 1.2 にしています。
3. **重くしすぎると悪化する。** `CUR_W=3.5` では検証区間が 1.06951 で、
   **今季をまったく使わない場合（1.06915）より悪くなります。**
   3-4章の `HALF_LIFE=6`（意外に長い）と同じ教訓で、
   「直近だけを見る」のはサッカーでは裏目に出ます。

### それでも「勢い」は扱っていません

`CUR_W` は「今季のデータをどれだけ信じるか」という**一律の重み**です。
今季の全試合を平等に `CUR_W` 倍で足すだけなので、
「直近5試合は好調」といった時間的な波は表現しません。

試合日が入ったので、**この検証は原理的に可能になりました**（22章）。
ただし7章の休養日数が「効果なし」だったのと同じく、
測ってみたら効かない可能性は十分にあります。**測ってから入れてください。**

---

## 15. 離脱者（けが・出場停止）の扱い

「発表があったものだけ、浅く反映する」方針です。

```
クラブごとに 選手名＋影響度（大 / 中 / 小）を登録
    大 -5.0% ／ 中 -2.5% ／ 小 -1.0%   （攻撃力の低下幅）
    クラブ合計の上限 -15%
    守備側への波及 = 低下幅 × 0.6      （失点が増える）
週中に別大会（ACL・天皇杯・ルヴァン） → 攻撃 ×0.97 ・ 失点 ÷0.97
```

```js
att = (1 - dep) * (mid ? 0.97 : 1)          // 自分の得点にかかる
def = (1 + dep * 0.6) * (mid ? 1/0.97 : 1)  // 相手の得点にかかる
λ(ホーム) = 素の期待得点 × 相性 × att(ホーム) × def(アウェイ)
```

**この数値は実測ではありません。** 7章の日程補正と同じく調整パラメータです。
選手単位のデータを持っていないので、効果量を推定するすべがありません。
だから効き幅を小さく取り、上限で頭打ちにしています。
「エースが1人抜けたら-5%」は、**そう決めた**という以上の根拠を持ちません。

### 離脱者補正を優勝/降格シミュレーションには使わない理由

シミュレーションは残り最大38節ぶんを一気に消化します。そこに「今週の離脱者」を
かけると、**その選手が6月まで離脱し続ける前提**になってしまいます。
そのため補正は**表示中の節の予想にだけ**かけ、シミュレーションでは外しています
（コード上は `predict(h, a, useCond)` の第3引数で切り替え）。

---

## 16. 優勝・降格確率（モンテカルロ）

やっていることは単純です。

```
1万回くり返す:
    残っている試合を1つずつ、ポアソン分布から乱数でスコアを引いて消化する
    入力済みの結果と合わせて最終順位を作る
    1位だったクラブ / 3位以内 / 下位3 を数える
数えた回数 ÷ 1万 = 確率
```

### 速くするための工夫

素直に書くと重いので2点だけ手を入れています。

1. **期待得点 λ は試合ごとに1回だけ計算する。** 試行のたびに `predict()` を呼ぶと
   3588試合の走査を何百万回もすることになります。先に λ を出し、
   そこから**累積確率の表**を作っておいて、乱数1つで得点を引きます（逆関数法）。
2. **順位づけは挿入ソート。** 20要素なので `Array.sort()` の呼び出しコストのほうが重くなります。

結果、**1万回で約0.6秒**で終わります（Node で実測）。
画面が固まらないよう500回ずつに区切って進捗を出しています。

### 検算

確率なので、合計が理論値に一致するかを必ず確かめます。

```
✅ 優勝確率の合計    = 1.000000  （優勝は1クラブ）
✅ 3位以内の合計     = 3.000000  （3枠）
✅ 降格確率の合計    = 3.000000  （3枠）
✅ 全クラブの平均勝点 = 52.0      （38試合・引分25%なら約52が期待値）
```

これが合わないときは、順位づけかカウントのどこかが壊れています。

### 注意

- 降格は**下位3クラブ**として計算しています（2026/27の入替は各リーグ間3クラブ）
- 「3位以内」はACL出場権の目安として置いたもので、実際の出場枠とは無関係です
- ここで出る確率は**モデルが正しければ**の話です。モデル自体の的中率は46%前後（6章）で、
  その不確かさは確率には含まれていません

---

## 17. 節別アプリの検証

方針は単発アプリと同じです。**HTMLからモデル部分だけを抜き出して Node で実行**し、
「表示している数字＝実際に動いているコードの数字」を保証しています。

**この検証は `node tools/verify.js` にまとめました。** 79項目を確認し、
1つでも落ちれば終了コード1で終わります。データやアプリを触ったら必ず通してください。
何を確認しているかの一覧は [data/README.md](data/README.md) にあります。要点だけ：

```
データ      3588試合 / 各シーズンの試合数 / 重複なし / 全試合に日付とスコア
            2026-27 は 380試合・38節・各節10試合・20クラブが1回ずつ
            順序付き380通りが重複なくそろう / 各クラブ ホーム19・アウェイ19
初期状態    リーグ平均得点 ホーム1.376・アウェイ1.212（+13.6%）
            ホーム勝率40.7% / 引分率25.0% / 平均総得点2.59
            履歴なしクラブ（水戸・千葉）が昇格クラブの平均像から始まる
            380試合すべてで 勝+分+敗=1、期待得点が 0.76〜2.11 に収まる
            鹿島ホームの鹿島勝率47.9% > 浦和ホームの鹿島勝率35.8%
整合性      HTMLの埋め込み3588件が data/j1-matches.json と1試合単位で一致
            HTMLの P・PROMOTED が data/params.json・promoted.json と一致
            FIXTURES_RAW が data/j1-2026.json と節・ホーム・アウェイまで一致
精度        params.json に書いた log loss が再現する（1.05486 / 45.84%）
```

以下は verify.js には入れていない（人が目で見る）確認です。

```
更新        水戸が5連勝 → 期待得点が上がり、順位表1位・勝点15になる
            1試合あたりの動きが暴れない（縮小推定が効いている）
離脱者      エース級離脱で自分の得点↓・相手の得点↑、8人でも-15%で頭打ち
シミュ      合計が 1 / 3 / 3、平均勝点52.0、1万回で約0.6秒
```

---

## 18. 節別アプリで手を動かす

### 難易度★

1. **順位表の列を並べ替えられるようにする**（勝点順／得点順／失点順）
2. **「今節の見どころ」を出す**
   → その節の10試合のうち、勝分敗の確率がいちばん均衡している試合を強調する
3. **各クラブの残り日程の難易度を出す**
   → 残り試合の相手の「戦力」の平均を取るだけ

### 難易度★★

4. **自分の予想の的中率を記録する**
   → 節ごとに予想確率を保存しておき、結果と突き合わせて log loss を計算する。
     14章のような検証を、自分のシーズンでリアルタイムにやることになります。
     **このアプリでいちばん学びが大きい改良です**
5. **順位の推移グラフを出す**
   → 節ごとの順位を折れ線で描く
6. **「優勝確率がこの1節でどう動いたか」を出す**
   → 前節終了時のシミュレーション結果を保存しておいて差分を表示する

### 難易度★★★

7. **`CUR_W` を消化率に応じて変える**
   → 試合日が入ったので、消化率を正確に出せるようになりました。
     消化率の関数として `CUR_W` を決め、**両区間で本当に良くなるか確かめる**
8. **「勢い」を扱えるようにする**
   → 試合日が入ったので**測れるようになりました**（25章）。
     ただし7章の休養日数が「効果なし」だったのと同じく、効かない可能性は高いです
9. ~~**昇格クラブをJ2の成績から評価する**~~
   → **完了（採用せず）。** 5章。水戸・千葉は今も「昇格クラブの平均像」から始まります

### 改良したら必ずやること

```
node tools/verify.js が全項目通ることを確認する。
そして log loss が「学習区間と検証区間の両方で」下がったか確認する。
片方だけなら、それは「良くなった気がする」だけです。
```

---

## 19. 節別アプリの限界

12〜18章で触れたものをまとめます。

| 限界 | 影響 |
|---|---|
| 離脱者の係数が実測でない | 「-5%」に根拠がない。効き幅を小さくして誤魔化している |
| 「勢い」を扱えない | 連勝中でも `CUR_W` 倍で平等に足されるだけ |
| 移籍が反映されない | 冬の移籍で戦力が変わっても、モデルは今季の結果からしか学べない |
| 監督交代が反映されない | 同上 |
| シミュレーションはモデルの不確かさを含まない | 「優勝確率30%」はモデルが正しい前提の値 |
| 日程データは取得時点のもの | 延期・会場変更は反映されません |

**離脱者の係数だけが、いまも実測されていない唯一の項目です。**
7章の日程補正は実測に置き換わり（効果なしと判明）、
13章の日程データも公式の単一ソースになりました。残るのは選手単位の情報です。

なお第20節に1試合だけ日付未定の試合があります（公式が未発表）。
節の順序は確定しているので予想には影響しません。

**特に「勢い」と「移籍」の欠落は、週次アプリとしては痛いところです。**
それでも、節が進めば今季の結果そのものがゆっくり反映されていくので、
シーズン後半の予想は前半よりは信頼できます（14章の表がそれを示しています）。

---

## 20. 注意（節別アプリ）

- データ出典：Ｊリーグ公式データサイト（`data.j-league.or.jp`）
  2026-27 対戦カード380試合（試合日つき）／2015-2025 J1 全3588試合
- 日程は取得時点（2026年8月）の情報です
- データを取り直す手順は [data/README.md](data/README.md)
- このアプリは**統計モデルによる推定**であり、結果を保証するものではありません
- **賭博等の用途を意図していません**

---
---

# データ層（back）— 2026年8月に作り直した記録

ここからは、アプリが使うデータをどう作っているかの話です。
**フロント（画面）を触る前に、データ側を実測で固めました。**

## 21. なぜ作り直したか

以前のデータは Wikipedia の戦績表（ホーム×アウェイのクロス表）でした。
これには**試合日が入っていません**。そのため

- 「中2日だったか」が分からず、日程補正は推測のままだった（旧7章）
- 「第5節終了時点」を再現できず、今季の重みはランダム並べ替えで近似していた（旧14章）
- 「直近5試合の調子」は原理的に測れなかった

つまり**データの穴が、そのままモデルの穴になっていました。**
パラメータをいくら調整しても、この3つは埋まりません。

そこで試合日を含むデータを探し、Ｊリーグ公式データサイトを見つけました（2章）。

## 22. いま何が実測で、何が推測か

| 項目 | 状態 | どこで測ったか |
|---|---|---|
| 攻撃力・守備力 | ✅ 実測 | 2015-2025 J1 全3588試合 |
| ホームアドバンテージ | ✅ 実測 | ホーム1.376 / アウェイ1.212（+13.6%） |
| 時間減衰 `HALF_LIFE` | ✅ 実測 | 日付順バックテスト＋分割検証（4章） |
| 縮小推定 `SHRINK` | ✅ 実測 | 同上 |
| 今季の重み `CUR_W` | ✅ 実測 | 日付順バックテスト（14章） |
| 対戦相性 `H2H_K` | ✅ 実測（**有害と判明**） | 3-6章 |
| Dixon-Coles `RHO` | ✅ 実測（**不要と判明**） | 3-7章 |
| 日程補正（中N日・別大会） | ✅ 実測（**効果なしと判明**） | 7章 |
| 昇格クラブの平均像 | ✅ 実測 | 25クラブ882試合（5章） |
| J2成績の持ち越し | ✅ 実測（**採用できずと判明**） | 5章 |
| 本拠地以外での開催 | ✅ 実測（記録のみ・未適用） | 23章 |
| **離脱者の係数** | ❌ **推測** | 選手単位のデータが無い（15章） |

**推測は1つだけになりました。** そして
「実測したら効かなかった」が4つあります。これは失敗ではありません。
**効かないものを入れないために測る**のであって、
測らなければ4つの誤った補正がそのまま残っていました。

### 実測して分かった一番大事なこと

```
近似や思い込みは、たいてい効果を大きく見せる。
```

- 日程補正：想定 -7% → 実測ほぼ0（符号すら逆向き）
- 相性補正：想定「効く」→ 実測「有害」
- 今季の重み：近似では最良1.5 → 実測では1.0〜1.5がほぼ同じ

## 23. 新しく手に入った項目を試した記録

公式データサイトからは、試合日以外に **K/O時刻・会場・入場者数** も取れます。
効くかどうか分からないので、測りました。

### 本拠地以外での「ホーム」試合

国立競技場での開催や、改修による一時移転です。3588試合中197試合（5.5%）あります。

```
                件数   ホーム勝率   得点
本拠地          3391    40.2%     1.362 - 1.209
本拠地以外        197    49.7%     1.629 - 1.254
```

生の数字では大きな差ですが、これは「強いクラブほど大きな会場を使う」偏りを含みます。
モデルの期待得点で割って地力を打ち消すと：

```
                件数   ホーム得点比   ホーム失点比
本拠地          2521     0.985        0.957
本拠地以外        149     1.092  ±0.068  1.039
```

**本拠地以外だとホームの得点が9%増えます。** 直感と逆ですが、
大きな会場に大観衆が入るぶんホームアドバンテージが強まる、と読めます。

バックテストでも、補正 ×1.04 が学習・検証の両区間で改善し、
やりすぎると悪化する（内点で最適になる）という健全な形をしていました。

```
  v      学習       検証     本拠地以外149試合だけ
 1.00   1.04598   1.06822        1.02409
 1.04   1.04593   1.06799        1.02198   ← 最良付近
 1.08   1.04606   1.06792        1.02280
 1.20   1.04727   1.06849        1.03991
```

**ただしアプリには入れていません。** 理由は3つです。

1. 全体への効果が 0.00012 と小さい（相性補正の修正の10分の1）
2. 対象が149試合しかなく、確信が持てない
3. `index.html` は「2クラブを選ぶ」だけで会場の概念がない。
   入れるなら会場データの引き回しと入力欄が必要で、**フロント側の作業になる**

実測値は `data/calib-venue.json` に残してあります。
フロントを触るときに使ってください。

### 曜日

```
曜日   件数   ホーム得点比  ホーム失点比
月       24    1.182        0.979
火       48    0.794        0.963
水      371    0.995        0.973
木        5    1.078        0.937
金      143    0.833        1.015
土     1383    1.025        0.977
日      696    0.961        0.915
```

月曜と火曜は件数が少なすぎて何も言えません。
水曜（371試合）は 0.995 で、平日開催そのものに偏りは見えませんでした。
金曜の 0.833 は気になりますが、143試合では誤差の範囲です。**採用しません。**

## 24. データ層の作り方

```bash
node tools/all.js         # ★これ1つで下の全部を順に実行する（約60秒）

node tools/fetch.js       # 公式サイトから取得（data/raw/ にキャッシュ）
node tools/parse.js       # raw/*.html → data/*.json
node tools/tune.js        # パラメータを決める → data/params.json
node tools/build.js       # HTMLの埋め込みデータと定数を作り直す
cd publish && node build.js ../節別予想.html   # 公開用ページを本体から作り直す
node tools/verify.js      # 検算（79項目）。全部通らないと完成ではない
```

**公開用ページを忘れないでください。** `publish/index.html` は
「本体（`節別予想.html`）＋ 結果の自動取得アドオン」でできています。
`publish/build.js` を**引数なしで**実行すると自分自身を元に組み直すので、
本体を作り直しても公開版は追いつきません。実際にそれで公開版だけ
旧パラメータ（`H2H_K=24` / `CUR_W=1.5`）と旧データのまま残っていました。
`verify.js` の7章がこの食い違いを検出します（`all.js` は正しい引数で自動実行します）。

設計で気をつけた点が3つあります。

**1. 取得と解釈を分ける。**
`fetch.js` は生HTMLを保存するだけ、`parse.js` は保存済みHTMLを読むだけです。
パースを直したいときに再取得が要りません（相手のサーバにも優しい）。

**2. モデルの実装を1つにする。**
`tools/lib/model.js` が唯一の実装で、バックテストもキャリブレーションもこれを呼びます。
アプリHTMLの定数がこことズレていないかは `verify.js` が突き合わせます。
さらに `verify.js` は**HTMLからモデル部分を切り出して Node で実行**し、
実際に動くことまで確認します。

**3. `build.js` を冪等にする。**
何度実行しても同じ結果になります。2回目は「変更なし」と表示されるだけです。
`--dry` を付けると差分だけ見られます。

### あとで触るときの注意

- **HTMLの `DATA` / `P` / `PROMOTED` / `FIXTURES_RAW` を手で書き換えないでください。**
  `data/` を直して `build.js` を通します。`verify.js` が食い違いを検出します。
- パラメータを変えたくなったら `tune.js` を回してください。
  手で決めた値は、たいてい過剰適合か思い込みです（4章）。
- 新しい補正を思いついたら、`backtest.js` の `run()` に渡す `tweak` で試せます。
  **学習区間と検証区間の両方で log loss が下がらなければ入れないでください。**
- シーズンが進んだら `node tools/all.js --refetch` を回すのがいちばん確実です
  （`fetch --force` → `parse` → `tune` → `build` → 公開版 → `verify` を順に実行します）。
- **公開版だけ更新し忘れる**のがいちばんやりがちな失敗です。
  `publish/build.js` には必ず `../節別予想.html` を渡してください。

## 25. まだやれること

試合日が入ったので、以前は「検証できないから入れない」としていたものが測れます。

### 難易度★★

1. **「勢い」を測る（旧18章の★★★ #8）**
   直近N試合の成績が、次の試合の結果を説明するかを実測する。
   7章の休養日数と同じ手順です。`backtest.js` の `tweak` に
   「直近5試合の得点比」を掛ける関数を渡し、両区間で log loss が下がるか見ます。
   **下がらない可能性が高い**と予想しますが、それを確かめること自体に価値があります。
2. **入場者数を使う**
   `att` が入っています。観客数はホームアドバンテージの強さと関係するかもしれません。
   ただし予想時点では未来の観客数は分からないので、
   「スタジアムの規模」や「昨季の平均」に置き換える工夫が必要です。
3. **K/O時刻（ナイトゲームか）を試す**
   `ko` が入っています。23章の曜日と同じ方法で測れます。

### 難易度★★★

4. **本拠地以外での開催を実際に適用する（23章）**
   実測値（×1.04）は出ているので、あとはフロント側の作業です。
5. **天皇杯・ACLの日程を集める**
   7章の結論は変わらないと考えていますが、確かめたければ。
   JFA公式はJS描画なので、Wikipedia の各回記事（`天皇杯 JFA 第105回全日本サッカー選手権大会`）
   から取ることになります。ACLは `AFCチャンピオンズリーグ2023/24` などの記事です。
6. **モデルを Dixon-Coles の最尤推定に置き換える**
   現在は「得点比の集計」という簡易法です。全パラメータを同時に最尤推定すると
   精度が上がる可能性があります。

### 改良したら必ずやること

```
node tools/verify.js が全項目通ることを確認する。
そして log loss が「学習区間と検証区間の両方で」下がったか確認する。
片方だけなら、それは「良くなった気がする」だけです。
```


---
---

# J2 と、Web への公開

## 26. 公開版（[publish/](publish/)）

手元の `節別予想.html` を Web に置いたものです。中身は同じですが、1点だけ違います。

**公開版は、今季の結果を Wikipedia から自動で取り込みます。**

```
publish/
  site/index.html   ← アップロードするのはこれだけ
  index.html        ← 同じもの（作業用）
  sync-addon.html   ← 自動取得の部品
  build.js          ← 「本体 ＋ アドオン」を繋ぐ
```

### なぜ本体を直接いじらないのか

`節別予想.html` は `tools/build.js` の**生成物**です。手で書き換えても次のビルドで消えます。
そこで自動取得は `sync-addon.html` に分け、**本体のコードには一行も触れていません**。
アドオンは本体から `LEAGUES` `STATE` `save()` `refit()` `renderAll()` を借りるだけです。

```bash
cd publish
node build.js ../節別予想.html   # 本体の最新版から公開版を組み直す
```

何度実行しても同じ結果になります（古いアドオンを剥がしてから付け直すため）。
`tools/all.js` はこれを正しい引数で自動実行し、`verify.js` の7章が
「アドオンを剥がした中身が本体と1文字単位で一致するか」を毎回確かめます。
**公開版だけ古いモデルのまま配る、という取り残しを機械で防ぐ仕組みです。**

### 取得元が Wikipedia である理由

出典としてはＪリーグ公式データサイトのほうが上です。しかし**ブラウザからは読めません**。

```
data.j-league.or.jp  → access-control-* ヘッダなし   ✗ ブラウザ不可（Node なら可）
ja.wikipedia.org     → access-control-allow-origin: *  ✓ ブラウザ可
```

サーバーを持たずにブラウザだけで完結させるなら Wikipedia 一択、というだけの理由です。
弱点は**編集者が更新するまで反映されない**ことなので、自分でスコアを入力する道も残し、
手で入れた値は自動取得で上書きしないようにしてあります。

### 戦績表から節を割り出す

Wikipedia の戦績表はクロス表なので**節を持ちません**。行は次の形です。

```
|match_ANT_RED=2-1        （鹿島 2-1 浦和）
|match_FMA_ANT={{small|8/7}}   ← 未消化。日付が入っている
```

そこで**日程から逆引き**します。「ホーム|アウェイ」→「第N節の何試合目」の対応表を作れば、
スコアだけ拾って正しい場所に入れられます。数字-数字の形だけを採るので、
日付・空欄・延期は自動的に未消化として扱われます。

---

## 27. J2 を足す

昇降格でクラブが入れ替わるので、J2 も見たいという話から追加しました。

### まず分かったこと：J2 は既に集めてあった

`tools/fetch.js` は最初から J2（2015-2026 の5,298試合）を取得していました。
**しかも「昇格クラブを J2 の成績で評価する」は既に実装され、実測で棄却されていました。**

```
data/promoted.json
  method  : prior = 昇格クラブ平均 + b × (J2での攻守力 - 1)
  採用値  : b = 0        ← つまり J2 成績を使わない
  verdict : "J2の成績を使っても精度は上がらなかった。昇格クラブ平均のまま使う"
```

「J2 で強いこと」と「J1 で通用すること」の相関は思ったより弱く、
**「昇格したクラブはだいたいこのくらい苦戦する」という平均のほうが当たる**、というのが実測の答えでした。

つまり **J2 を足しても J1 の精度は上がりません。** 上がるのは「J2 そのものを予想できる」という別の価値です。
足す前にこれを確かめたので、期待値を間違えずに済みました。

### モデルはそのまま載った

`tools/lib/model.js` の `fitRatings(past, cur, season, P, prior, hasHistory)` は
**リーグに依存しません**。試合の配列を渡すだけです。J1・J2 とも 20クラブ・38節・1節10試合なので、
画面の作りもそのまま使えました。

---

## 28. J2 のパラメータを測り直す

J1 の値を流用したくなりますが、**流用してよいかどうかを測っていない**のが問題です。
そこで `tools/tune-j2.js` が、J1 の確定値を出発点にして、J1 とまったく同じ規則
（学習 2018-2022 と 検証 2023-2025 の**両方**で log loss が下がったときだけ採用）で決め直します。

### 結果：3つとも動いた

| | HALF_LIFE | SHRINK | RHO | H2H_K | CUR_W |
|---|---|---|---|---|---|
| J1 | 6 | 28 | 0 | 80 | 1.2 |
| **J2** | **5** | **18** | 0 | 80 | **1.8** |

```
SHRINK       28 →   18   学習 -0.00012  検証 -0.00090
CUR_W       1.2 →  1.8   学習 -0.00180  検証 -0.00018
HALF_LIFE     6 →    5   学習 -0.00039  検証 -0.00002
```

**3つとも「J2 は J1 より今季の情報を重く見るべき」という同じ方向を向いています。**

- `CUR_W` が大きい（1.8 対 1.2）＝ 今季の1試合を重く見る
- `HALF_LIFE` が短い（5 対 6）＝ 過去を早く忘れる
- `SHRINK` が小さい（18 対 28）＝ 平均へ寄せる力を弱める

J2 は毎年6クラブが入れ替わる（J1から3・J3から3）ので、**古い成績があてにならない**わけです。
理屈としては後付けですが、**3つが独立に同じ向きへ動いた**のは偶然にしては出来すぎています。
流用していたら気づけませんでした。

### 精度：J2 のほうが当てにくい

```
        log loss   的中率    基準率（常にホーム）      試合数
J1      1.05485    45.84%    1.07991 / 41.16%    2670
J2      1.06321    43.83%    1.08972 / 38.70%    3532
一様（1/3ずつ）  1.09861    33.33%
```

どちらも基準率より良いので**モデルは効いています**が、**J2 のほうが読みにくい**。
これも「入れ替わりが激しく、前提が毎年崩れる」ことの表れだと考えられます。

### J2 の「新顔クラブ」

J1 の昇格クラブに相当するものですが、J2 の新顔には2種類あります。

- **J1 から落ちてきたクラブ**（強いことが多い）
- **J3 から上がってきたクラブ**（弱いことが多い）

J3 のデータを持っていないので個別には分けられません。そこで
「J2 に初めて出たシーズンの成績」を全部ならした平均像を使います（21クラブ・シーズン／870試合）。

```
atkH 1.0238   defH 1.0684   atkA 1.0787   defA 1.0410
```

攻撃は平均並みなのに失点が多い、という混ざった姿になっています。
**強い落下組と弱い昇格組が打ち消し合った結果**で、個別に見れば全然違うはずです。
J3 を足せばここを分けられます（30章）。

---

## 29. 途中で踏んだ罠

### 29-1. リファクタでモデルを静かに壊した

J2 でもバックテストできるよう、`backtest.js` の中身を `lib/backtest-core.js` に切り出しました。
そのとき1行だけ書き換えました。

```js
for (const m of [...matches, ...cup]) m.day = dayNum(m.date);              // 元
for (const m of [...matches, ...cup]) if (m.day == null) m.day = dayNum(m.date);  // 変えた
```

「既に入っているなら残す」ほうが親切に見えます。**これで精度が壊れました。**

`parse.js` が書き出す JSON の `day` は **「第N節第N日」の N（1〜6）**であって、日付ではありません。
元のコードは無条件に日付通番へ上書きしていたので成立していました。
条件を足した結果、`day` が 1 や 2 のまま残り、**日付順の並べ替えと「その試合より前」の判定が壊れた**のです。

エラーは一切出ません。出たのは違う数字だけでした。

```
tune.js を再実行したら
  H2H_CAP 0.25 → 0.05 ／ CUR_W 1.2 → 1.5 ／ 日程補正 0 → 0.1
```

### なぜ気づけたか

**変更前のコードで先に `tune.js` を回して、`params.json` が1文字も変わらないことを確かめてあったからです。**
その基準があったので「変わったのは自分のせい」と即座に切り分けられました。
基準を取らずに進めていたら、「J2 対応したらパラメータが変わった。まあそういうこともある」で
通してしまったはずです。

**リファクタの前に「変えないはずのものが変わらない」ことを確かめる。**
この一手間が、今回いちばん効きました。core には理由をコメントで残してあります。

### 29-2. クラブの略称を推測したら半分間違えた

Wikipedia の戦績表は3文字コードでクラブを表します。J2 のぶんを埋めるとき、
一度は知識から書きました。**20件中9件が誤りでした。**

```
推測: JEF="千葉"（実際はJ1）、八戸が抜けている、
      BLB/IWT/KTP/FUJ/TSG/TMZ/KOF/TOK … 実際は VEG/JUB/KAT/MYF/SAG/TEG/VEN/VOR
```

しかも**間違えても静かに動きます**。知らないコードの行は読み飛ばされるだけで、
「そのクラブの試合だけ入らない」という気づきにくい壊れ方をします。

そこで推測をやめ、**機械的に確定させました**。手がかりは
「各クラブのホーム/アウェイの並び（38節ぶん）」です。これはクラブごとに一意なので、
Wikipedia 側のコードの並びと突き合わせれば対応が決まります。

```
Wikipedia のコード 20件 / 公式のクラブ 20件
各クラブのホーム/アウェイの並びは一意
確定 20/20
検算: 一致 380件 / 不一致 0件
```

**推測ではなく、一致するかどうかで確かめられる形にしたのが要点です。**

さらに、将来また入れ替わったときのために**取りこぼしの検出**を足しました。
知らないコードや日程と対応しない行があれば、画面に警告を出します。

### 29-3. 検算のほうが間違っていた

J2 で「ホームアドバンテージが効いているか」の検算が落ちました。
いわきのホーム勝率 34.4% に対し、アウェイ勝率 40.4%。逆転しています。

調べると**バグではありませんでした**。いわきは本当にアウェイの方が強いクラブです。

```
いわき  atkH 1.000 / defH 1.098      ホームでは攻撃も守備も平凡以下
        atkA 1.098 / defA 0.965      アウェイでは攻撃も守備も良い
```

3-3章のとおり、このモデルは**クラブごとにホームとアウェイを別々に持ちます**。
「ホームでだけ強いクラブ」を表現できるということは、その逆も表現できるということです。
J1 でも380カード中26件で同じ逆転が起きます。

誤っていたのは「全カードでホーム有利が成り立つ」と決めつけた検算のほうでした。
リーグ全体で見る形に直しています。

```
✅ リーグ平均でホームの方が得点が多い（J1 +14.9% / J2 +11.5%）
✅ 大半のカードでホーム側が有利になる（8割以上）
```

**検算が落ちたとき、まず疑うべきはコードとは限りません。**

### 29-4. 表示した数字が古いまま残っていた

画面の「仕組み」に `CUR_W=1.5`・的中率 46.4%・log loss 1.0565 と書いてありました。
**どれも測り直す前の値**です（正しくは J1 で 1.2・45.84%・1.0548）。

原因は単純で、そこだけ手書きだったからです。`tools/build.js` が
`params.json` / `params-j2.json` から組み立てるようにしました。
**測り直せば表示も追いつきます。**

6章に「表示した数字を自分のコードで再検証する」と書いておきながら、
説明文のほうが検算の対象外になっていた、というだけの話でした。

---

## 30. これからやれること

### 次のシーズン（2027-28）へ

年が埋まっているのは5ファイルです。日程の発表（例年6月頃）を待って直し、`node tools/all.js` を1回。

| ファイル | 箇所 |
|---|---|
| `tools/fetch.js` | 取得年の範囲 |
| `tools/parse.js` | 学習／予想の境界（`s <= 2025` と `s === 2026`）と出力ファイル名 |
| `tools/build.js` | 読み込むファイル名 |
| `tools/calibrate-promoted.js` | 昇格クラブを判定する年 |
| `publish/sync-addon.html` | Wikipedia の記事名と、クラブの3文字コード |

**26-27 の結果は自動的に学習データへ移ります。** 境界を1年進めるだけです。
公式サイトが全380試合のスコアを返すので、手入力は要りません。
`tune.js` / `tune-j2.js` がパラメータを測り直すので、1シーズン増えたぶん精度も更新されます。
水戸・千葉も「昇格クラブの平均像」ではなく実際の J1 成績で評価されるようになります。

**3文字コードの更新を忘れても、29-2 の警告が知らせます。**

### J3 について（アプリには載せない判断）

**データはすでに集まっています。** `data/j3-matches.json` に 2015-2026 の3,666試合、
26-27シーズンの日程380試合も揃っており、`tools/calibrate-j3.js` もあります。
Wikipedia の「2026/27シーズンのJ3リーグ」も J1・J2 と同じ形式で存在します。

それでも**アプリの画面には載せていません**。理由は精度の見立てです。
27章のとおり、J2 の成績を使っても J1 の精度は上がりませんでした。
1階級隣の J2 ですら効かないのだから、**2階級離れた J3 はさらに遠い**と考えられます。

つまり J3 を足して得られるのは「J1・J2 の予想が良くなること」ではなく、
**「J3 そのものを予想できる」「J2 の新顔クラブを落下組と昇格組に分けられる」という別の価値**です。
それが要るときに載せればよく、いまは見送っています。

**「取れるから入れる」ではなく「効くか測ってから決める」**——
3-7章で Dixon-Coles 補正に対してやったのと同じ判断です。

### 難易度★★

1. **自分の予想の的中率を記録する** — 節ごとに予想確率を保存し、結果と突き合わせて log loss を計算する。
   28章と同じ検証を、自分のシーズンでリアルタイムにやることになります
2. **J1・J2 をまたいだ表示** — 「J2で昇格圏のクラブは、J1なら何位くらいか」を出す。
   両リーグの攻守力を同じ物差しに載せる必要があり、**そこが難所**です
3. **順位の推移グラフ**

### 難易度★★★

4. **「勢い」を扱う** — 直近N試合の成績が次の試合を説明するか実測する。
   14章で「検証できないから入れなかった」と書いた部分を、試合日が入ったいま測り直せます
5. **J3 のデータで J2 の新顔クラブを分ける** — 28章の「混ざった平均像」を落下組と昇格組に分解する
6. **昇降格を跨いだクラブの連続性** — J1で降格したクラブがJ2でどう振る舞うかを、
   リーグをまたいで1つのレーティングとして扱う

### 改良したら必ずやること

```
node tools/all.js     … データ取得〜生成〜検算まで通す
```

`verify.js` の104項目が通ること、そして
**学習区間と検証区間の両方で log loss が下がったときだけ採用すること**。
片方だけの改善は雑音です（4章）。
