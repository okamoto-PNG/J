# 公開のしかた

[publish/site/](publish/site/) を置けば「リンクを送れば誰でも見られるURL」になります。

```
tools/publish.js       ← 公開ページを組み立てる（ソース）
tools/sync-addon.html  ← 試合結果を自動で取り込む機能（ソース）
        ↓ 生成
publish/index.html     ← 中身の確認用
publish/site/          ← 配る用。このフォルダだけ置けばよい（index.html 1つだけ）
```

**`publish/` は生成物だけの置き場です。** ソースは `tools/` にあります。
ソースを生成物の中に置くと、`publish/` を消した瞬間に作り直せなくなるためです
（GitHub Actions は `publish/` を持たない状態から始まるので、これが必須でした）。

サーバー処理なし・ビルド環境不要。開いた人のブラウザが Wikipedia から今季の結果を読み、
戦力・順位表・優勝／降格確率を勝手に更新します。データは見た人のブラウザにだけ保存されるので、
**誰が開いても自由に触れて、他の人には影響しません**。

---

## 作り直しかた

```bash
node tools/publish.js            # 本体（節別予想.html）から作り直す
node tools/publish.js <file>     # 別のファイルを本体として使う
```

`node tools/all.js` の中でも自動的に呼ばれるので、ふだんは意識しなくてよいです。
何度実行しても同じ結果になります（古いアドオンを剥がしてから付け直すため）。
実行時に、本体との噛み合わせ・個人情報の混入・外部リソース参照を自動で確認し、
問題があれば止まります。

**以前は `publish/build.js` に引数を渡す形でした。** 引数を忘れると自分自身を元に
組み直してしまい、公開版だけ古いモデルで残る事故が起きました。
いまは既定で本体を読むので、引数を忘れても壊れません。

> **⚠ 引数を省くと本体に追いつきません。**
> 引数なしの `node build.js` は**今の index.html 自身**を元にアドオンを付け直すだけです。
> データ層を作り直したあとにこれだけ実行すると、**公開版だけ古いモデルのまま**になります。
> 実際にそれで旧パラメータ（`H2H_K=24` / `CUR_W=1.5`）と旧データが残っていたことがあります。
>
> いまは `node ../tools/verify.js` の7章が「アドオンを剥がした中身が本体と1文字単位で一致するか」を
> 確かめるので、忘れれば検算が落ちます。`node ../tools/all.js` は正しい引数で自動実行します。

### なぜ本体を直接いじらないのか

**本体は生成物なので、手で書き換えても次のビルドで消えます。**
そこで自動取得の機能は `sync-addon.html` に分けてあり、本体のコードには一切触れていません。
アドオンは本体から `FIXTURES` `STATE` `save()` `refit()` `renderAll()` を借りるだけです。
本体でこれらの名前が変わったら `build.js` が検知して止まります。

### 結果の取得元について

出典としてはＪリーグ公式データサイトのほうが上ですが、
**CORS ヘッダを返さないのでブラウザからは読めません**（Node からなら読めます）。

```
data.j-league.or.jp  → access-control-* なし   ✗ ブラウザ不可
ja.wikipedia.org     → access-control-allow-origin: *   ✓ ブラウザ可
```

サーバーを持たずにブラウザだけで完結させるなら Wikipedia 一択、というのが理由です。
**Wikipedia の編集者が更新するまで反映されない**という弱点があるので、
急ぐときは自分でスコアを入力できるようにしてあります。
自分で入れた値は、あとから自動取得で勝手に書き換えられません。

---

## 方法A：Netlify Drop（いちばん速い。5分かからない）

アカウントを作らなくてもURLが出ます。**URLにあなたの名前が入りません。**

1. ブラウザで https://app.netlify.com/drop を開く
2. この `publish` フォルダを、ページの点線の枠に**ドラッグ＆ドロップ**
3. 数秒で `https://なにか-ランダム.netlify.app` というURLが出る → 完成

### 注意

- アカウント無しのままだと、そのサイトは**一時的なもの**として扱われます。
  URLを配って使い続けるなら、表示される「Claim this site」から無料アカウントを作って
  自分のものにしてください（GitHub/メールでサインアップ）。
- サイト名は後から `設定 → Site details → Change site name` で好きな名前に変えられます。
  例：`https://j1-yosou-2627.netlify.app`

### 更新のしかた

`index.html` を直したら、同じサイトの `Deploys` タブにフォルダを再度ドラッグするだけ。URLは変わりません。

---

## 方法B：GitHub Pages（ずっと置いておくならこちら）

`https://<GitHubユーザー名>.github.io/<リポジトリ名>/` というURLになります。
**URLにGitHubのユーザー名が入る**ので、それが嫌なら方法Aにしてください。

### 事前に一度だけ：コミットに残る名前を確認する

**gitのコミットには、作者の名前とメールアドレスが記録されます。**
公開リポジトリに push すると、それも公開されます。
このフォルダは**リポジトリ単位で差出人を仮の値に設定済み**なので、
パソコン全体の設定（勤務先のメールアドレスなど）は混ざりません。

GitHubのアカウントに紐づけたい場合だけ、次を実行してください。
`<...>` の部分はGitHubの `Settings → Emails` に書かれている noreply アドレスです。

```bash
git config user.name  "あなたのGitHubユーザー名"
git config user.email "1234567+ユーザー名@users.noreply.github.com"
```

`@users.noreply.github.com` を使えば、**本当のメールアドレスは公開されません**。

### 手順

1. GitHub で新しいリポジトリを作る（**Public**。README等のチェックは全部外す）
2. このフォルダで次を実行

```bash
git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
git branch -M main
git push -u origin main
```

3. リポジトリの `Settings → Pages` を開く
4. `Source` を **Deploy from a branch**、`Branch` を **main / (root)** にして Save
5. 1〜2分待つと、同じ画面にURLが出る → 完成

### 更新のしかた

```bash
git add -A
git commit -m "更新内容"
git push
```

---

## どちらを選ぶか

| | Netlify Drop | GitHub Pages |
|---|---|---|
| 手間 | ドラッグ&ドロップだけ | リポジトリ作成 + push + 設定 |
| URLに名前が入るか | 入らない | GitHubユーザー名が入る |
| 更新 | 再ドラッグ | `git push` |
| 履歴が残るか | 残らない | 残る（学習向き） |

**とりあえずリンクを配りたいだけなら方法A**、
**gitの練習も兼ねたいなら方法B**、という選び方で問題ありません。

---

## 公開する前に知っておくこと

- **データは見た人ごとに別々です。** あなたが入力した結果は、自動では相手に見えません。
  相手が開くと、まっさらな第1節から始まります。
- **予想だけは共有できます。** ページ内の「予想を共有する」で作ったリンクを送ると、
  開いた人の画面にあなたの予想が取り込まれ、的中率が並んで表示されます。
  送られるのは**予想と名前だけ**で、試合結果や離脱者の設定は入りません。
  ただし**自動では集まりません**（誰かが送る操作をする必要があります）。
- **締切は各試合のキックオフです。** 過ぎると入力欄が閉じ、締切後に入れた予想は的中率から除外されます。
  K/O時刻が未発表の試合は当日0時が締切になります。
- **締切を証明したいときは「封印コード」を使います。** キックオフ前に節ビューで「封印する」を押し、
  出たコード（例 `a3f9-21c8-7d04`）をグループLINE等に貼ってください。試合後に予想を共有すると、
  受け取った側がコードを照合できます。**後から予想を書き換えるとコードが合いません。**
  コードはハッシュなので、貼った時点では中身が分かりません。
  **時刻の根拠はチャットの投稿時刻**です（アプリは時刻を証明しません）。
- 端末を引っ越したいときは「書き出し（JSON）」の文字列を送ってください（結果ごと丸ごと移ります）
- ページには**あなたの名前・所属・連絡先は一切入っていません**
- 出典（Ｊリーグ発表の対戦カード／Wikipedia の戦績表）と
  「賭博などの用途を意図していません」という注記はページ下部に入っています
- 予想の的中率は46%程度です。ページ内の「仕組みと、どこまで信用していいか」に根拠を書いてあります
