# 引き継ぎ書

Jリーグ試合予想アプリ。**別のセッションで再開するときは、まずこれを読んでください。**

最終更新 2026-08-05 / 最新コミット `c31529e`（CIの自動更新）/ 検算 **206項目すべて合格**

**運用がクラウドに移りました。** 正はリポジトリ側です。手元で `all.js` を回さないでください
（同じ生成物を2箇所で書き換えて必ず食い違います）。詳しくは2節。

---

## 1. まず動かして状態を知る

```bash
cd "<このフォルダ>"
node tools/verify.js        # 206項目。全部通れば健全
node tools/auto.js --dry    # いま何をするか（何も書き換えない）
node tools/check-cloud.js   # クラウド側が動いているか
```

`verify.js` が全部通ることが**「完成」の定義**です。何かを変えたら必ず通してください。

いまの状態（`data/season.json` より）

```
予想対象 2026-27 / 学習 2015-2025 / 学習区間 2018-2022 / 検証区間 2023-2025
J1  log loss 1.05485  的中率 45.84%（2670試合）
J2  log loss 1.06127  的中率 44.28%（3532試合）
```

---

## 2. いちばん近い「次にやること」

**次の月曜 12:00(JST) の自動実行が緑になるのを見届ける。** それだけです。
[クラウド化手順.md](クラウド化手順.md) の**全ステップが完了しました**（2026-08-05）。

```
1. 差出人の設定    ✅ anonymous <noreply@example.com>（実メールは入っていない）
2. コミット        ✅ 87d4a3c
3. push            ✅ github.com/okamoto-PNG/J（Public）
4. Pages の Source ✅ GitHub Actions
5. dry → auto      ✅ どちらも成功。自動コミット c31529e まで確認
6. ローカルのタスク ✅ 削除済み（二重実行の危険は解消）
```

確認は1コマンドです。

```bash
node tools/check-cloud.js
```

まだ**手動実行しか通っていません**。タイマーで走ることの確認は次の月曜が初回です。
`check-cloud.js` の「うち毎週の自動実行」が1件以上になれば、放っておいても回る状態です。

公開ページ <https://okamoto-png.github.io/J/>

### 運用がこう変わりました

| | これまで | いま |
|---|---|---|
| 正 | 手元のフォルダ | **リポジトリ** |
| 更新 | 毎週ローカルのタスク | 毎週 CI が実行してコミット |
| 手元で見るとき | そのまま開く | **`git pull` してから**開く |
| 公開ページ | 手で Netlify にドラッグ | 自動で差し替わる |

**手元で `all.js` を回さないでください。** CI と同じ生成物を書き換えるので必ず衝突します。
手元は「読む・コードを直す」用です。コードを直したら push すれば、次回の CI がそれで走ります。

### 改行コードの注意（1回踏んだ）

手元の git は `core.autocrlf=true` なので、ローカルからコミットすると `data/raw/*.html`
（16MB）が LF に正規化され、Linux の CI がコミットしたものと**全行差分**になります。
移行時に1回起きました。**生の HTML を手元からコミットしない**限り再発しません。

---

## 3. この環境の罠（知らないと必ず時間を溶かす）

**実際に踏んで、原因を突き止めたものだけを書いています。**

| 罠 | 症状 | 対処 |
|---|---|---|
| `fs.rmSync` / `fs.cpSync` が OneDrive 配下で**プロセスごと落ちる** | 例外ではなく異常終了（0xC0000409）。**stderr も出ない**。Git Bash では exit 127 | `fs.unlinkSync` と明示的な `copyFileSync` を使う |
| PowerShell 5.1 は BOM なし UTF-8 の `.ps1` を ANSI と誤読 | 「文字列の終端がない」という謎の構文エラー | `.ps1` は **UTF-8 BOM 付き**で保存 |
| `Get-Content` の既定エンコーディング | ログが文字化けする | `-Encoding utf8` を付ける |
| git がマルチバイトのファイル名をエスケープする | `"\347\257\200..."` になりファイルが見つからない | `git -c core.quotepath=false` |
| Git Bash の `/c/...` を node に渡せない | `C:\c\Users\...` を開こうとして ENOENT | node にはWindows形式のパスを渡す。長いワンライナーはファイルに書いてから実行 |
| Ｊリーグ公式サイトが 503 を返す | 取得が集中するとしばらく全滅。**「まだ日程が無い年」と区別できない** | 失敗は記録して次回に持ち越す（`fetch.js` がそうしてある） |
| 別セッションが同じファイルを触っていた | 読んだ直後に内容が変わる | 編集前に読み直す。`verify.js` の項目数が勝手に増えていることがある |

---

## 4. 壊してはいけない設計（守るべき不変条件）

### 年を直書きしない

`data/season.json` が**唯一の正**です。`tools/lib/season.js` が導き方を持ち、`parse.js` が書きます。
下流（tune / calibrate / build / verify / アプリHTML）は**そこだけを読みます**。

```
予想対象   = まだ結果が無い試合がある最後のシーズン
学習の終わり = 予想対象の1つ前
採点開始   = 最初のシーズン + 3
検証区間   = 消化済みの最後3季
学習区間   = 採点開始 〜 検証区間の直前
締切の基準日 = 予想対象シーズンの最も早い試合日
```

**`2026` や `38` や `20クラブ` を新しく書かないでください。** 来季また全部直すことになります。

### パイプラインの順序に意味がある

```
fetch → parse → tune(1周目) → calibrate-promoted → calibrate-j3
      → tune(2周目・確定) → tune-j2 → calibrate-schedule → calibrate-venue
      → build → publish → verify
```

パラメータと事前分布は**互いを必要とする**ので2周します。1周だと
`params.json` に記録した精度が再現しなくなります（来季を模擬して見つけた）。

### 採用規則

新しい補正・パラメータは**学習区間と検証区間の両方で log loss が下がったときだけ**採用します。
片方だけの改善は雑音として捨てます。この規則で、日程補正・H2H・Dixon-Coles・
J2→J1の持ち越しは「効かない」と判明して入れていません。

### publish/ は生成物だけ

ソースは `tools/publish.js` と `tools/sync-addon.html` です。
`publish/` は親リポジトリで追跡していないので、**ソースを中に置くと CI がビルドできません**。

### 個人情報の検出語をソースに書かない

`data/leak-words.txt`（**追跡外**）に置きます。検出語をソースに並べると、
公開した時点で**それ自体が漏洩**になります。汎用パターンだけ `tools/lib/leaks.js` にあります。

検算 7-b 章が、そのファイルが追跡されていないことと、追跡ファイル全部に固有語が
無いことを毎回見ています。**落ちても語そのものは出しません**（検算のログが漏洩源に
なるため）。固有語は CI に無いので、**CI のログは1つ少ない205項目**になります。

### 自動更新は「失敗したら元に戻す」

`tools/auto.js` は実行前に生成物をOSの一時領域へ退避し、検算が落ちたら全部戻します。
`verify.js` の 6-d 章が**わざとファイルを壊して復元されるか**を毎回確かめています。
効かない安全網は無いのと同じなので、ここは必ず検算対象にしてください。

### モデルの採点で未来を使わない

予想の的中率を出すとき、モデルは**その節より前の結果だけ**で学習し直します（`fitBefore`）。
後から入れた結果でモデルを作ると「未来を見て予想した」ことになり精度が嘘になります。

---

## 5. ファイルの地図

```
index.html        2クラブを選んで1試合を予想（単発型・J1のみ）
節別予想.html      毎節の予想・採点・共有・封印（週次型・J1とJ2）★アプリ本体
publish/site/     配る用の公開ページ（生成物。CIが作る）

data/season.json  ★シーズン境界。下流はここだけ読む
data/*.json       学習データ・パラメータ・実測記録
data/raw/*.html   取得した生HTML（48ファイル・16MB。触らない）

tools/all.js          全部を順に実行
tools/auto.js         ★自動更新。判定・退避・復元
tools/verify.js       ★全検算（206項目）
tools/lib/season.js   シーズン境界の導き方
tools/lib/model.js    モデル本体（唯一の実装）
tools/lib/leaks.js    公開してはいけない語の検出
tools/publish.js      公開ページの組み立て
tools/check-cloud.js  クラウド側の状態を見る
tools/setup-schedule.ps1  毎週実行の登録（PCを変えたらこれ1回）

解説.md            ★なぜそうなっているかの全記録（31章）
data/README.md     ★データ層の手順・検算の一覧
クラウド化手順.md    GitHub Actions への移行手順
公開手順.md         公開ページの作り方
AI予想.md          別系統の実験（統計モデルとは無関係・精度未検証）
```

**迷ったら `解説.md` を読んでください。** 失敗した試みも「なぜ採用しなかったか」まで
残してあります。同じ失敗を繰り返さないための記録です。

---

## 6. まだやっていないこと

| 項目 | 状況 |
|---|---|
| 毎週の自動実行の確認 | 手動実行は緑。**タイマー実行は次の月曜が初回**（2節） |
| 離脱者の係数 | 唯一の「推測」。選手単位のデータが無いので実測できない |
| 本拠地以外での開催（×1.04） | 実測済みだがアプリ未適用。会場の入力欄が必要 |
| 「勢い」（直近N試合） | 未実測。`backtest.js` の `tweak` で試せる。効かない可能性が高い |
| J3 単体の予想画面 | データはある。ただし J3 はクラブ数が13〜20と動くので作りを直す必要あり |
| 封印の締切 | 節単位（節の最初のキックオフ）。試合単位にすると1節で10個貼ることになり非現実的 |
| 予想共有の自動集約 | サーバーが無いので手作業。締切の**証明**は封印コードで解決済み |

---

## 7. 直近4セッションでやったこと（背景）

0. **クラウドへ移行（2026-08-05）** — push 前に、追跡ファイル102件・19.4MB 全部に
   `leaks.js` の見張りを掛けた（検算7章は `publish/` しか見ていないため）。
   固有語の検出は**0件**。`OneDrive` 7件は罠の説明文、`C:\` 52件は生HTML内の
   New Relic の正規表現 `code:\d+` の誤検出で、いずれも実害なし。
   この走査は使い捨てにせず、**検算 7-b 章として常設**した（201 → 206項目）。
   わざと固有語を足して落ちることまで確かめてある。
   Pages を有効にする前に `auto` を回すと `deploy` だけが 404 で落ちる（`update` は緑）。
1. **公開版の取り残しを修正** — `publish/build.js` を引数なしで実行すると自分自身を元に
   組み直すため、公開版だけ旧パラメータ（`H2H_K=24`）で残っていた。検算7章を追加。
2. **J3 を追加** — J2 の新顔を出自（J1から/J3から）で分けて評価。`atkH` で 1.22 対 0.85 の
   差があり、混ぜてはいけなかった。J2全体で log loss 1.06321 → 1.06127。
   「2階級離れたJ3は効かないはず」という見立ては**外れた**。
3. **予想の採点・共有・締切・封印** — 自分の予想を入れて採点、リンクで共有、
   キックオフ締切、ハッシュをチャットに貼って締切を証明する仕組み。
4. **年の直書きを全廃 → 自動更新** — 来季を模擬して4件のバグを発見・修正。
   毎週の自動実行をタスクスケジューラに登録し、GitHub Actions の用意まで済ませた。

---

## 8. 相談の前提として共有したいこと

- **身内で楽しむ規模**を想定しています。賭けごとの用途は意図していません。
- 予想の共有・封印は**自己申告**が土台です。チャットの投稿時刻を証拠にしていますが、
  意図的なごまかしは防げません。それで十分という判断です。
- 数字は必ず**測ってから**入れています。「定番だから」で入れたものはありません。
- ドキュメントには**失敗と、見立てが外れた記録**も残しています。消さないでください。
