# publish/ は独立したリポジトリ（公開用の成果物）なので、ここでは追わない。
# 中身は publish/ 側の git が持っている。
/publish/

# エディタ・OS が勝手に作るもの
.DS_Store
Thumbs.db
*.swp
node_modules/

# 自動更新（tools/auto.js）の実行記録。中身は環境ごとに違うので追わない
data/auto-log.txt

# 個人情報の検出語。これ自体が個人情報なのでリポジトリに入れない（tools/lib/leaks.js）
data/leak-words.txt
