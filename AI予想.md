# AI予想（実験）

`tools/ai-yosou.js` は、**毎回その場で公式データを読み込んで、Claude に試合を予想させる**スクリプトです。

`index.html` / `節別予想.html` の統計モデルとは**まったく別のもの**です。置き換えではありません。

---

## まず前提：これは精度が保証されていません

このリポジトリの他の部分は「実測して、効かないものは入れない」で作られています
（[解説.md](解説.md) の日程補正の章がその典型で、**効果が検出できなかったので入れませんでした**）。

**このスクリプトはその基準を満たしていません。** 検証していないからです。

| | index.html / 節別予想.html | AI予想 |
|---|---|---|
| 中身 | ポアソン分布 + 時間減衰の統計モデル | 大規模言語モデル（Claude） |
| 精度 | 3588試合でバックテスト済み（的中率46%程度） | **未検証** |
| 再現性 | 同じ入力なら常に同じ出力 | 実行ごとに変わりうる |
| 根拠の説明 | パラメータを全部開示 | モデルが書いた文章。検算できない |

一般に、**LLMは確率の較正が得意ではありません。**「45%」と言ったことが本当に45%の頻度で起きるか、
このスクリプトは何も保証しません。統計モデルの方が log loss で勝つ可能性が高いと考えてください。

なぜ作ったか：「数字を渡したらLLMは何と言うか」を見るためです。学習用の実験です。

---

## 使い方

### 1. APIキーを用意する

<https://platform.claude.com/> でキーを作り、環境変数に入れます。

```powershell
# PowerShell（そのセッションだけ有効）
$env:ANTHROPIC_API_KEY = "sk-ant-..."
```

```bash
# Git Bash
export ANTHROPIC_API_KEY="sk-ant-..."
```

**キーはファイルに書かないでください。** このリポジトリを公開したときに一緒に出ていきます。

### 2. 実行する

```bash
node tools/ai-yosou.js                          # 次の節（未実施の最も早い節）を全部
node tools/ai-yosou.js --round 3                # 節を指定
node tools/ai-yosou.js --home 鹿島 --away 浦和   # カードを指定
node tools/ai-yosou.js --dry                    # 送る内容だけ表示。APIを呼ばない（キー不要）
node tools/ai-yosou.js --no-fetch               # 取得せず data/*.json だけで作る
node tools/ai-yosou.js --model claude-sonnet-5  # 安いモデルを使う
```

結果はターミナルに出て、`AI予想.html` にも書き出されます（1ファイルで完結・外部読み込みなし）。

**まず `--dry` を実行してください。** キーを使わずに「何を渡しているか」が全部見えます。

### 3. コスト

実行ごとに概算を表示します。1試合あたり入力1500トークン前後なので、

| モデル | 1試合 | 1節（10試合） |
|---|---|---|
| `claude-opus-5`（既定） | 約 $0.014 | 約 $0.14 |
| `claude-sonnet-5` | 約 $0.008 | 約 $0.08 |

1節ぜんぶ予想しても20円程度です。

---

## 毎回データを読み込む、の中身

実行するたびに、公式データサイトへ**2リクエスト**投げます（`data/raw/` にキャッシュしません）。

| 取るもの | なぜ |
|---|---|
| J1 2026-27 | 今季の日程と、そこまでの結果。節が進めば自動で反映される |
| ルヴァンカップ 2026 | 直前の公式戦を知るため（中N日の計算に必要） |

2015-2025 の3588試合は変わらないので `data/j1-matches.json` をそのまま使います。取り直しません。
`--no-fetch` を付けると通信せず、`data/*.json` だけで動きます（オフラインで試せます）。

パーサは `tools/parse.js` の `parseHtml()` を借りています。**パーサはこの1箇所だけです。**
2つに分けると必ず片方が古くなるので、コピーしていません。

---

## モデルに渡している材料

`--dry` で実物が見られます。渡しているのはこれだけです。

- 対戦カード・節・日付・K/O時刻・会場
- **J1の基準率**（2015-2025の実測：ホーム勝ち40.7% / 引分25.0% / アウェイ勝ち34.3%、平均得点 H1.376 / A1.212）
- 両クラブの今季成績（順位・勝点・得失点）と、ホーム／アウェイ別の成績
- 両クラブの直近5試合（勝敗・スコア・H/A・相手）
- 両クラブの前季（2025）の順位と成績 — **昇格クラブは J2 の成績**
- 直前の公式戦から中N日（リーグ戦＋ルヴァン杯から計算）
- 直接対戦の履歴（最大8件）

### 渡していないもの

移籍・負傷・出場停止・監督交代・天候・モチベーション。**取得元にないので渡せません。**
システムプロンプトで「渡されていない情報は使うな」と明示していますが、
LLMは学習データ由来の古い知識を混ぜることがあります。これは検算できません。

### 出力の受け取り方

構造化出力（`output_config.format` の JSON Schema）で受けています。文章から数字を拾っていません。

3つの確率の合計は「1にしろ」と指示していますが、モデルは外すことがあります。
**こちら側で正規化し、元の合計がどれだけずれていたかを表示します**（`※ 合計が 1.030 だったので正規化した`）。
ずれが大きいときは、モデルが確率として扱えていない兆候です。

---

## 検算

```bash
node tools/verify-ai.js     # 164項目。APIは呼ばない
```

予想の当たり外れは検算できません。できるのは「**渡している数字が正しいか**」だけです。それを164項目確認します。

- 2025シーズンの最終順位を再現するか（鹿島1位・勝点76、リーグ全体の得点合計＝失点合計、引分数が偶数…）
- ホーム別＋アウェイ別が通算と一致するか（全20クラブ×3項目）
- 直近5試合の勝敗記号がスコアと矛盾しないか
- 直接対戦がホーム／アウェイを入れ替えても同じ試合を返すか
- 昇格クラブが J2 の成績で拾えるか（昇格数と降格数が一致するか）
- 確率の正規化が比を保つか／合計0で例外になるか
- プロンプトに `undefined` や `NaN` が混ざらないか

シーズン序盤は今季の消化試合が0件なので、順位表のコードは一度も動きません。
そのため **2025シーズンを「今季」に見立てて第10節で切り、シーズン中の経路も通しています。**

---

## 依存パッケージ

このスクリプトだけ `@anthropic-ai/sdk` を使います。他のツールは無依存のままです。

```bash
npm install     # 初回のみ
```

`node_modules/` は `.gitignore` 済みです。

---

## 分かっていること・分かっていないこと

**分かっていること**

- 実行ごとに最新データを読み、材料は164項目の検算を通っている
- 構造化出力なので、数字の取り出しで壊れることはない
- 確率の合計は必ず1になる（正規化しているので）

**分かっていないこと**

- 当たるのかどうか。**一度も検証していません**
- 較正されているか。「45%」が本当に45%なのか不明
- 実行ごとにどれだけ答えがぶれるか（同じ入力で複数回試すと分かります）
- 学習データ由来の古い知識をどれだけ混ぜているか

**次にやるなら**：節が消化されたら結果を突き合わせて log loss を出す。
`tools/backtest.js` が統計モデルで同じ指標を出しているので、直接比べられます。
AIが負けたとしても、それが分かること自体がこのリポジトリの流儀に合っています。

---

出典：[Ｊリーグ公式データサイト](https://data.j-league.or.jp/SFMS01/)。
賭博などの用途を意図していません。学習用に作成。
