# クラウド化の手順（GitHub Actions で毎日自動更新）

PC の電源が落ちていても毎週更新されるようにします。**準備は全部済んでいます。**
残っているのは「GitHubにリポジトリを作って push する」ところだけで、そこは手で行います
（この環境に `gh` コマンドが無く、認証情報も持っていないためです）。

所要 15〜30分。

---

## 0. 先に確認したこと

| 項目 | 結果 |
|---|---|
| CIと同じ状態（追跡ファイルだけ）で動くか | ✅ 確認済み（`publish/` が無い状態から公開ページ216KB） |
| 検算 | ✅ 全206項目合格 |
| 追跡対象に氏名・勤務先・ユーザー名が混ざっていないか | ✅ 無し（`data/leak-words.txt` は `.gitignore` 済み） |
| 依存パッケージ | パイプラインは**無依存**（`tools/ai-yosou.js` だけが使う） |

---

## 1. コミットする名前を決める（1回だけ）

**公開リポジトリでは、コミットの作者名とメールアドレスも公開されます。**
本当のアドレスを出さないため、GitHub の noreply アドレスを使ってください。
`Settings → Emails` に書かれています。

```bash
cd "<このフォルダ>"
git config user.name  "<GitHubのユーザー名>"
git config user.email "<数字>+<ユーザー名>@users.noreply.github.com"
```

`--global` を付けないでください。**このリポジトリだけ**の設定にします。

---

## 2. まとめてコミットする

```bash
git add -A
git status          # 入るものを目で確認（下の「入らないもの」に注意）
git commit -m "シーズン境界の自動導出・予想の採点と共有・毎週の自動更新を追加"
```

**入らないもの**（`.gitignore` 済み。意図どおりです）

```
publish/              生成物。CI が毎回作り直す
data/auto-log.txt     実行記録。環境ごとに違う
data/leak-words.txt   個人情報の検出語。これ自体が個人情報
node_modules/
```

---

## 3. GitHub にリポジトリを作る

1. <https://github.com/new> を開く
2. 名前は任意（例 `j-league-yosou`）
3. **Public** を選ぶ ← GitHub Pages を無料で使うため
4. README などのチェックは**全部外す**（すでに中身があるので衝突します）

```bash
git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
git branch -M main
git push -u origin main
```

`data/raw` が16MBあるので、最初の push は少し時間がかかります。

---

## 4. Pages を有効にする

`Settings → Pages` で

```
Build and deployment
  Source: GitHub Actions      ← 「Deploy from a branch」ではありません
```

`Source` を **GitHub Actions** にしないと、ワークフローの公開手順が失敗します。

---

## 5. 動かしてみる

`Actions` タブ →「自動更新」→ `Run workflow`

| 選ぶもの | 何をするか |
|---|---|
| `dry` | 判定だけ。何も書き換えない。**まずこれ** |
| `auto` | ふだんの動作（毎週これが走る） |
| `rebuild` | 取得せずキャッシュから作り直す |
| `force` | 全年を取り直す |

`dry` が緑になったら `auto` を1回。終わると Pages のURLが出ます。

---

## 5-b. 「クラウド側が動いた」をどう判断するか

**1コマンドで分かります。**

```bash
node tools/check-cloud.js
```

見ているのは3つです。

```
── 1. ワークフローの実行 ──────────────────
  ✅ 成功  2026-08-10 12:03 JST  毎週の自動
  ✅ 成功  2026-08-05 15:20 JST  手動

  成功 2/2件  うち毎週の自動実行 1件

── 2. 自動コミット ────────────────────────
  ✅ 2026-08-10 12:04 JST  自動更新: 2026-27 時点のデータで作り直した

  手元を最新にするには: git pull

── 3. 公開ページ ──────────────────────────
  ✅ 見られます: https://<ユーザー名>.github.io/<リポジトリ名>/
     216KB / 予想対象シーズン 2026

══════════════════════════════════════════
クラウド側は動いています。
```

### 「動いた」の基準

| 段階 | 見るところ | 意味 |
|---|---|---|
| 仕組みが動く | 1 が **手動で ✅** | ワークフロー・Pages の設定が正しい |
| タイマーも動く | 1 に **「毎週の自動」で ✅** | 放っておいても走る。**ここまで見てから 6 に進む** |
| 公開ページ | 3 が ✅ | リンクを配れる |

### 間違えやすいところ

**「2. 自動コミット」が空でも失敗ではありません。**
データが変わらなかった週は、何もしないのが正しい動作です。
判断は **1 の結果（緑か）** で行ってください。

**「毎週の自動」は次の月曜まで出ません。** 手動実行では `event` が「手動」になります。
急ぐなら手動の ✅ だけで 6 に進んでも構いませんが、
1週間待って自動実行の ✅ を見てからの方が確実です。

ブラウザで見るなら、リポジトリの **Actions** タブに同じことが出ています。

---

## 6. ローカルの自動更新を止める（重要）

**両方動かすと、生成物を2箇所で書き換えて必ず食い違います。**
`node tools/check-cloud.js` が「クラウド側は動いています」と言ってから、
Windows のタスクを消してください。

```powershell
powershell -ExecutionPolicy Bypass -File tools\setup-schedule.ps1 -Remove
```

以後の運用が変わります。

| | これまで | これから |
|---|---|---|
| 正 | 手元のフォルダ | **リポジトリ** |
| 更新 | 毎週ローカルで実行 | 毎週 CI が実行してコミット |
| 手元で見るとき | そのまま開く | `git pull` してから開く |
| 公開ページ | 手で Netlify にドラッグ | **自動で差し替わる** |

`git pull` を忘れると手元が古いままになります。**手元でも `all.js` を回すと衝突します**
（どちらも同じ生成物を書き換えるため）。手元は「読む・コードを直す」用と考えてください。

---

## 7. うまくいかないとき

| 症状 | 見るところ |
|---|---|
| 公開手順で失敗する | `Settings → Pages` の Source が **GitHub Actions** か |
| コミットで失敗する | リポジトリの `Settings → Actions → General` で **Read and write permissions** か |
| 取得が失敗する | 公式サイトの 503（レート制限）。次週に拾うので放置してよい |
| 検算が落ちる | `auto.js` が元に戻しているのでデータは無事。ログに落ちた項目が出ます |

---

## 変えたくなったら

- **時刻** … `.github/workflows/auto-update.yml` の `cron: "0 3 * * *"`（UTC。JSTは+9時間）
  毎日にしてあるのは、発表されたK/O時刻を早く取り込むためです（変化が無ければ何もしません）
- **やめる** … ワークフローのファイルを消すか、Actions タブで Disable
